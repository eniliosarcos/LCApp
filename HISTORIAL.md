# Historial de Cambios — Mi Catálogo

Este archivo es el historial oficial del proyecto. Todo cambio que se realice sobre el código o la configuración debe registrarse aquí antes de darse por terminado.

## Regla

1. Agregar una entrada al inicio de la sección `## Historial` (la más reciente arriba).
2. No editar ni borrar entradas previas.
3. Si el cambio toca varios archivos, listar los relevantes.
4. El skill `change-history` implementa y hace cumplir esta regla.

## Formato

| Campo | Valor |
|---|---|
| Fecha | `AAAA-MM-DD` |
| Tipo | `feat` / `fix` / `refactor` / `config` / `docs` / `test` / `style` |
| Descripción | Qué se hizo y por qué |
| Archivos | Rutas afectadas |
| Decisión clave | Patrón o decisión importante (opcional) |

## Historial

### 2026-08-24 — style(admin): alinear visualmente tabla de fiados con resto del admin
- **Descripción**: (1) La tabla de fiados usaba `.credit-table` con estilos locales, fuera de los shared tables. Ahora usa `_tables.scss` compartido: base de tabla completa + `tr.clickable` hover + `.order-link` hover. (2) Badges de status `unpaid`/`partial`/`paid` migrados a tokens SCSS (`$color-danger-border`, `$color-warning-border`, `$color-success-border`). (3) Eliminado duplicado de `tr.clickable`/`.order-link` de `orders.component.scss`. (4) Toggle de venta crédito en el modal "Registrar venta".
- **Archivos**: `src/app/admin/styles/_tables.scss`, `src/app/styles/_variables.scss`, `src/app/admin/pages/credit-sales/credit-sales.component.scss`, `src/app/admin/pages/sales/manual-sale-modal/manual-sale-modal.component.*`
- **Decisión clave**: Unificar tablas del admin bajo un único shared `_tables.scss`. Tokens `-border` para badges de status (consistente con `$color-danger-border` existente).

### 2026-08-24 — feat(backend+admin): flujo de venta a crédito (fiado)
- **Descripción**: (1) Modelo Order extendido con `paymentStatus` (not_applicable/unpaid/partial/paid), `amountPaid`, `payments[]`. (2) Nuevo source `'fiado'` en Order. (3) Backend: 3 endpoints nuevos — `POST /api/orders/credit` (registrar fiado, descuenta stock, código `FIA-XXXXX`), `GET /api/orders/credit` (listar con filtros + totalPending), `POST /api/orders/:id/payments` (registrar abono con validación de monto vs pendiente). (4) Admin: página lista de fiados con resumen de deuda total, tabla con cliente/teléfono/total/pagado/pendiente/estado, filtros y paginación. (5) Admin: página detalle del fiado con info cliente, items, historial de pagos, formulario de abono con validación de monto máximo. (6) Sidebar: nuevo link "Fiados". (7) Órdenes existentes no afectadas — paymentStatus default `not_applicable`.
- **Archivos**: `backend/models/Order.js`, `backend/routes/orders.js`, `src/app/core/models/order.model.ts`, `src/app/core/services/order.service.ts`, `src/app/admin/pages/credit-sales/` (nuevo), `src/app/admin/pages/credit-detail/` (nuevo), `src/app/admin/admin-routing.module.ts`, `src/app/admin/admin.module.ts`, `src/app/admin/layout/admin-layout.component.html`
- **Decisión clave**: Extender Order con campos de crédito en vez de modelo separado — un fiado es una orden con tracking de pagos. Misma infraestructura, cero duplicación. Validación de abono: backend rechaza si `amount > remaining`.

### 2026-08-22 — feat(backend+ui): SKU auto-generado — el admin ya no necesita entender SKUs
- **Descripción**: (1) Backend auto-genera SKU al crear producto: prefijo de 3 letras de la categoría + secuencial de 3 dígitos (ej: `MAQ-001`). (2) PUT mantiene SKU existente si el admin no lo toca; auto-genera si el producto no tiene uno (legacy). (3) Índice MongoDB `sparse: true` para no colisionar con nulls. (4) Admin form: campo SKU read-only por defecto con botón "Editar" para override manual. (5) Detalle de producto: `*ngIf` para no mostrar "SKU:" vacío.
- **Archivos**: `backend/utils/slugify.js` (+`generateSkuPrefix`, +`uniqueSku`), `backend/models/Product.js`, `backend/routes/products.js`, `src/app/admin/pages/products/products.component.ts`, `src/app/admin/pages/products/products.component.html`, `src/app/catalog/pages/product-detail/product-detail.component.html`
- **Decisión clave**: SKU es 100% transparente para el admin — se genera solo. Si quiere usar su propio sistema de códigos, puede editarlo. Formato: primeras letras de categoría + secuencial.

### 2026-08-22 — feat(backend): logging estructurado con pino para eventos críticos
- **Descripción**: (1) `pino` como librería de logging estructurado (JSON en prod). Logs críticos: startup/conexión DB, creación/confirmación/cancelación de órdenes, decremento de stock, uploads/eliminación de imágenes R2, intentos de login fallidos, token inválido, errores en todas las rutas. (2) `logger.warn` en 400s de reglas de negocio (stock insuficiente, transiciones de estado inválidas). (3) Todas las rutas devuelven mensajes genéricos al client; `err.message` ya no se filtra (antes: leak de info interna). (4) `categories.js` y `config.js` refactorizados para usar logger. (5) `unhandledRejection` handler para Express 4. (6) Bug fix en `middleware/auth.js`: `err.message` → `err` para stack trace completo.
- **Archivos**: `backend/lib/logger.js` (nuevo), `backend/server.js`, `backend/routes/orders.js`, `backend/routes/images.js`, `backend/routes/products.js`, `backend/routes/auth.js`, `backend/routes/categories.js`, `backend/routes/config.js`, `backend/middleware/auth.js`, `backend/lib/r2.js`, `backend/package.json`, `backend/.env.example`
- **Decisión clave**: Solo logging de eventos críticos (no request logging de categorías/productos). Env var `LOG_LEVEL` para controlar nivel (default: `info`).
- **ADR**: `docs/adr/010-logging-estructurado.md`

### 2026-08-22 — refactor(ui): category strip extraído a componente compartido + cart auto-clear + fix paramMap
- **Descripción**: (1) `CategoryStripComponent` extraído a `shared/components/category-strip/` — detecta categoría activa vía `router.url`, chips responsivos, scroll con flechas. (2) `HomeComponent` simplificado: inline strip reemplazado por `<app-category-strip>`. (3) `ProductListComponent`: eliminado breadcrumb, agregado strip, fix crítico `snapshot.paramMap` → `paramMap` observable con `switchMap` + `forkJoin`. (4) Auto-clear del carrito en `AppComponent.ngOnInit`: si hay `orderCode` registrado, consulta status vía `OrderService` y limpia carrito si `confirmed`/`cancelled`. (5) `CartService.getCartSnapshot()` — nuevo método síncrono.
- **Archivos**: `shared/components/category-strip/` (nuevos: ts, html, scss), `shared/shared.module.ts`, `home/pages/home/home.component.*`, `catalog/pages/product-list/product-list.component.*`, `app.component.ts`, `app.component.spec.ts`, `core/services/cart.service.ts`
- **Decisión clave**: Category strip detecta categoría activa parseando `router.url` (no ActivatedRoute), permitiendo reutilización en home y product-list sin inyectar ActivatedRoute a nivel de layout.

### 2026-08-21 — feat(ui): lightbox pinch-to-zoom custom + pan + double-tap
- **Descripción**: Reemplazado el `touch-action: none` global por un handler custom de zoom: (1) pinch-to-zoom con 2 dedos (1x a 4x), (2) pan con 1 dedo cuando está zoomed, (3) double-tap para toggle 1x/2x, (4) zoom reset al cerrar lightbox o cambiar de imagen. Los controles (X, flechas, counter) no se ven afectados por el zoom porque solo la imagen recibe `transform: scale()`.
- **Archivos**: `product-gallery.component.ts`, `product-gallery.component.html`, `product-gallery.component.scss`
- **Decisión clave**: `touch-action: none` en `.lightbox__img` (solo imagen) permite control total del zoom. El lightbox container no tiene `touch-action`, así que el swipe back del navegador sigue funcionando.

### 2026-08-21 — fix(ui): lightbox — overflow pegado con back button + pinch-to-zoom rompe controles
- **Descripción**: (1) `ngOnDestroy` en `ProductGalleryComponent` restaura `document.body.style.overflow` si el componente se destruye con el lightbox abierto (back button en mobile). (2) `touch-action: none` en `.lightbox` desactiva pinch-to-zoom del navegador en el overlay, evitando que los controles (close, arrows, counter) se pierdan al hacer zoom.
- **Archivos**: `product-gallery.component.ts`, `product-gallery.component.scss`

### 2026-08-21 — feat(ui): lightbox fade+scale y cart item slide-out
- **Descripción**: (1) Lightbox de galería con animación de entrada (fade + scale 0.95→1, 250ms) y salida (fade + scale inverso, 150ms) vía two-phase close con `setTimeout`. (2) Cart item con slide-out a la izquierda + fade al quitar: `removing` flag con delay de 250ms antes de emitir el evento, `overflow: hidden` en `:host` para clip limpio.
- **Archivos**: `product-gallery.component.ts`, `product-gallery.component.html`, `product-gallery.component.scss`, `cart-item.component.ts`, `cart-item.component.html`, `cart-item.component.scss`
- **Decisión clave**: Lightbox usa dos-phase close (first `closing` class → animation out → then `lightboxOpen = false`) para animar salida. Tests actualizados a `fakeAsync`/`tick` para manejar los delays.

### 2026-08-21 — style(ui): micro-animaciones CSS (search focus, botón carrito, crossfade galería)
- **Descripción**: Tres mejoras CSS puras: (1) search bar focus con `outline-color` transition en vez de outline instantáneo, (2) botón "Agregar al carrito" con hover transition + `:active` scale(0.97) para feedback de press, (3) crossfade de imagen en galería de producto con `opacity` transition al cambiar de imagen.
- **Archivos**: `home.component.scss`, `product-detail.component.scss`, `product-gallery.component.scss`

### 2026-08-21 — feat(ui): animaciones sutiles para vista del cliente
- **Descripción**: Sistema completo de animaciones CSS + Angular solo para rutas del cliente (no admin): tokens compartidos (`_animations.scss`), transiciones de ruta fade (200ms), hero fade-in, stagger de product cards (60ms entre cada una), y scroll reveal directive (`IntersectionObserver`). Todo con `prefers-reduced-motion` para accesibilidad.
- **Archivos**: `styles/_animations.scss` (nuevo), `styles/_scroll-reveal.scss` (nuevo), `app/animations.ts` (nuevo), `shared/directives/scroll-reveal.directive.ts` (nuevo), `app.module.ts`, `app.component.ts`, `app.component.html`, `app-routing.module.ts`, `shared.module.ts`, `home.component.scss`, `home.component.html`, `product-detail.component.html`, `cart-view.component.html`
- **Decisión clave**: Sin librerías externas. CSS keyframes + Angular `@angular/animations` (ya instalado sin usar). Admin excluido de animaciones.

### 2026-08-21 — style(ui): padding-bottom en home, catalog y product-detail
- **Descripción**: `padding-bottom: 1rem` en `.catalog` y `.product-detail`. En home, `padding-bottom` del contenedor `.home` ajustado a `3.25rem` en mobile (antes `4.25rem`) para compensar la strip fija inferior.
- **Archivos**: `product-list.component.scss`, `product-detail.component.scss`, `home.component.scss`

### 2026-08-21 — feat(ui): "ver más" condicional en product-card
- **Descripción**: El enlace "ver más" en la descripción del producto solo se muestra cuando el texto se truna realmente (detectado vía `scrollHeight > clientHeight` en `ngAfterViewInit`). Para descripciones cortas no aparece el enlace.
- **Archivos**: `product-card.component.*`
- **Decisión clave**: Detección de truncado por comparación de dimensiones reales del DOM con `OnPush` + `markForCheck()`. Test de layouts extensos imposible en unit tests (NG0100) — se validó solo el caso negativo.

### 2026-08-20 — style(home): buscar productos arriba del título
- **Descripción**: La barra de búsqueda por nombre en el home ahora aparece arriba del título de la sección ("Todos los productos") en vez de debajo. Reordenamiento HTML sin cambios de SCSS.
- **Archivos**: `src/app/home/pages/home/home.component.html`

### 2026-08-20 — refactor(ui): gallery sin carousel, lightbox + simplificación carousel
- **Descripción**: Revertido el carousel del detail page. `ProductGalleryComponent` ahora muestra imagen principal + thumbnails clickeables + lightbox (`max-width: 90vw; max-height: 85vh; object-fit: contain`). Carousel simplificado: eliminados `variant`, lightbox, thumbnails. Solo queda para product-cards en home (dots, autoplay 1.5-5s, IntersectionObserver).
- **Archivos**: `product-gallery.component.*`, `product-image-carousel.component.*`
- **Decisión clave**: Separar responsabilidades: carousel solo para home cards, gallery con lightbox propio para detail. Lightbox con tamaño natural limitado a viewport para respetar proporción original sin cubrir toda la pantalla.

### 2026-08-18 — fix(images): unique folder per upload to prevent image overwrite
- **Descripción**: Al subir múltiples imágenes para el mismo producto, la segunda sobreescribía la primera en R2 porque ambas generaban la misma clave (`products/<slug>/400w.webp`). Fix: cada upload ahora incluye un fragmento UUID de 8 caracteres en la carpeta (`products/<slug>-a3f2b1c8/400w.webp`). Esto garantiza unicidad sin afectar uploads existentes.
- **Archivos**: `backend/routes/images.js`
- **Decisión clave**: UUID fragment corto (8 chars) en vez de timestamp — suficiente para unicidad, mantiene paths legibles.

### 2026-08-19 — feat(ui): carousel de imágenes en product-card y gallery
- **Descripción**: Nuevo componente shared `ProductImageCarouselComponent` con carousel de imágenes, auto-play cada 4s, pausa en hover/touch, dots clickeables, swipe con CSS scroll-snap, y pausa inteligente con IntersectionObserver. Integrado en product-card (home grid) y product-gallery (detail page). Cuando hay 1 imagen, se muestra sin carousel. Gallery simplificado: eliminados thumbnails y lógica de selección manual.
- **Archivos**: `src/app/shared/components/product-image-carousel/` (nuevo), `shared.module.ts`, `product-card.component.*`, `product-gallery.component.*`
- **Decisión clave**: Carousel shared reutilizable para ambos contextos. CSS scroll-snap nativo para swipe (sin librería). `IntersectionObserver` para pausar auto-play off-screen.

### 2026-08-18 — config — Sync prod→dev DB y R2
- **Descripción**: Sincronización completa de datos de producción a desarrollo. DB: categories (12) + products (24) copiados de `lcapp` a `lcapp-dev` via script Node.js con mongoose. R2: 45 imágenes migradas de `lcapp-images` (prod) a `lcapp-images-dev` (dev) descargando via URL pública y subiendo con credenciales dev. URLs en MongoDB dev actualizadas para apuntar al bucket dev.
- **Archivos**: `backend/sync-prod-to-dev.js`, `backend/sync-r2-prod-to-dev.js`, `.gitignore`
- **Decisión clave**: Scripts con credenciales hardcoded → gitignored (`backend/sync-*.js`). Download+upload en vez de CopyObject cross-bucket por restricciones de permisos de tokens R2 separados.

### 2026-08-18 — feat — Truncar descripción en product-card con "ver más"
- **Descripción**: Se limitó la descripción de las product cards a 4 líneas usando CSS line-clamp. Cuando la descripción se trunca, se muestra un indicador "ver más" en el color primario. Detección de truncado vía `scrollHeight > clientHeight` en `AfterViewInit`. La card completa ya es un link al detalle, así que "ver más" es solo indicador visual.
- **Archivos**: `src/app/shared/components/product-card/product-card.component.ts`, `.html`, `.scss`, `.spec.ts`
- **Decisión clave**: Se usó `@ViewChild` + `AfterViewInit` para detectar truncado real (no siempre mostrar "ver más"). El `*ngIf="isTruncated"` evita mostrar el indicador en descripciones cortas.

### 2026-08-18 — feat — Búsqueda por nombre en home (cliente)
- **Descripción**: Se agregó un campo de búsqueda por nombre en la página principal del catálogo, ubicado entre la cinta de chips de categoría y el grid de productos. Filtrado en tiempo real con `[(ngModel)]`. El getter `filteredProducts` busca solo por nombre de producto (no categoría). Búsqueda normalizada (sin tildes/caracteres especiales). El término de búsqueda persiste al cambiar de categoría. Mensaje contextualizado cuando no hay coincidencias.
- **Archivos**: `src/app/home/pages/home/home.component.ts`, `.html`, `.scss`, `.spec.ts`, `src/app/home/home.module.ts`
- **Decisión clave**: Se eligió buscar solo por nombre (no por categoría) porque la navegación por categoría ya existe con los chips. Se persiste el término al cambiar de categoría (Opción B) para que el usuario pueda comparar resultados.

### 2026-08-18 — feat — Búsqueda por nombre y categoría en admin productos
- **Descripción**: Se agregó un campo de búsqueda por nombre o categoría en la página de productos del admin, ubicado entre los filtros de stock y la tabla. Filtrado en tiempo real con `[(ngModel)]`. El getter `filteredProducts` compina filtro de stock + búsqueda por nombre/categoría. Búsqueda normalizada (sin tildes/caracteres especiales) con `normalizeText`. Mensaje contextualizado cuando no hay coincidencias.
- **Archivos**: `src/app/admin/pages/products/products.component.ts`, `src/app/admin/pages/products/products.component.html`, `src/app/admin/pages/products/products.component.spec.ts`
- **Decisión clave**: `filteredProducts` se encadena sobre `visibleProducts` (ya filtra por stock), manteniendo la composición de filtros. Se usa `normalizeText` para búsqueda case-insensitive y sin diacríticos.

### 2026-08-18 — fix — LOW_STOCK_THRESHOLD: 10 → 4
- **Descripción**: Se redujo el umbral de stock bajo de 10 a 4. Con stock promedio de 2-6 unidades, el badge "¡Últimas X unidades!" se mostraba en todos los productos, perdiendo efectividad. Ahora solo aparece en productos con stock ≤ 4.
- **Archivos**: `src/app/core/models/product.model.ts`, `src/app/shared/components/product-card/product-card.component.spec.ts`, `src/app/admin/pages/products/products.component.spec.ts`
- **Decisión clave**: El umbral de 10 era genérico; con inventario real de pocas unidades por producto, 4 es el punto donde el badge tiene impacto real.

### 2026-08-18 — feat — Valor total de inventario en admin
- **Descripción**: Se agregó el valor total del inventario (suma de `price × stock`) como subtítulo en el page header de la página de productos del admin. Getter computed `inventoryValue` calcula el total a partir del array `products` ya cargado, sin llamadas al backend adicionales.
- **Archivos**: `src/app/admin/pages/products/products.component.ts`, `.html`, `.scss`, `.spec.ts`
- **Decisión clave**: Se ubicó en el header de la página de productos (no en el dashboard) para respetar la separación de responsabilidades: inventario es tema de productos, no de órdenes. Sin cambios en backend ni modelos.

### 2026-08-18 — fix — Login: botón loading no se resetea en error
- **Descripción**: Al fallar el login (credenciales incorrectas), el botón quedaba en estado loading indefinidamente. Causa: el callback `error` de RxJS no ejecutaba `this.loading = false`; solo `complete` lo hacía, y `complete` no se ejecuta cuando hay error.
- **Archivos**: `src/app/auth/pages/login/login.component.ts`
- **Decisión clave**: Agregar `this.loading = false` en el callback `error` del subscribe.

### 2026-08-18 — config — Carga de datos reales: 12 categorías y 24 productos
- **Descripción**: Se limpiaron las bases de datos de prod (`lcapp`) y dev (`lcapp-dev`) y se cargaron datos reales del negocio: 12 categorías de maquillaje y 24 productos (Dolce Bella, Aria Cosmetics, Salomé, Sheglam, Prosa, MaxGlow, Trendy) con precios en USD y stock. Se crearon `backend/clean.js` (limpieza directa contra MongoDB) y `backend/seed-real.js` (carga vía API con login JWT). Se descubrió que `sku: { unique: true }` en el modelo de Producto sin `sparse: true` bloquea múltiples productos sin SKU (índice único no sparse trata `null` como duplicado); se resolvió asignando SKUs únicos a cada producto.
- **Archivos**: `backend/clean.js` (nuevo), `backend/seed-real.js` (nuevo)
- **Decisión clave**: Datos reales se cargan vía API (no seed directo a MongoDB) para validar la misma lógica que usa el admin. Los scripts son de uso único; no se ejecutan en CI ni en deploy.

### 2026-08-17 — config — Rename de Cloudflare Pages: lcapp → lessencerise
- **Descripción**: Se creó un nuevo proyecto `lessencerise` en Cloudflare Pages (renombrar el viejo solo cambia el nombre visual, no la URL). Se actualizaron referencias en README.md, ARCHITECTURE.md y backend/.env.example (`CORS_ORIGIN`). Los buckets R2 (`lcapp-images` / `lcapp-images-dev`) no cambiaron.
- **Archivos**: `README.md`, `docs/ARCHITECTURE.md`, `backend/.env.example`
- **Decisión clave**: Cloudflare Pages no permite cambiar la hostname de un proyecto existente; la solución es crear uno nuevo y apuntar al mismo repo.

### 2026-08-17 — refactor — Unificar moneda a USD: eliminar CurrencyFormatPipe custom
- **Descripción**: Se eliminó `CurrencyFormatPipe` (custom, locale `es-MX`, default `MXN`) y se unificó toda la app con el pipe nativo `| currency` de Angular (default USD `$`). Se reemplazaron 10 ocurrencias de `| currencyFormat` por `| currency` en templates de catalog, cart y product-card. Se eliminó el archivo `shared/pipes/currency-format.pipe.ts` y su declaración de `SharedModule`. Se actualizaron 9 specs quitando el import y declaración del pipe custom; en `product-card.spec.ts` se reemplazó `new CurrencyFormatPipe().transform()` por strings literales `$100.00`/`$80.00`.
- **Archivos**: `src/app/shared/pipes/currency-format.pipe.ts` (eliminado), `src/app/shared/shared.module.ts`, `src/app/catalog/pages/product-detail/product-detail.component.html`, `src/app/shared/components/product-card/product-card.component.html|spec.ts`, `src/app/cart/components/cart-item/cart-item.component.html|spec.ts`, `src/app/cart/components/cart-summary/cart-summary.component.html|spec.ts`, `src/app/admin/pages/dashboard/dashboard.component.spec.ts`, `src/app/admin/pages/sales/sales.component.spec.ts`, `src/app/admin/pages/orders/orders.component.spec.ts`, `src/app/admin/pages/products/products.component.spec.ts`, `src/app/admin/pages/order-detail/order-detail.component.spec.ts`
- **Decisión clave**: El pipe custom nacío por la necesidad de locale `es-MX` con moneda `MXN`. Con USD ya no aporta valor; el pipe nativo de Angular da `$1,234.56` con cero config. El `formatCurrency()` de `manual-sale-modal` se mantiene (usa `Intl.NumberFormat` en código TypeScript, fuera del alcance de pipes Angular).
- **Verificación**: `ng build --configuration production` OK; `ng test --watch=false` **216/216 OK**.

### 2026-08-17 — style — Formato de hora 12h en panel admin
- **Descripción**: Las fechas en el panel admin ahora muestran la hora en formato 12 horas con AM/PM (`hh:mm a`) en vez de formato 24 horas (`HH:mm`). Aplica a order-detail, dashboard y orders.
- **Archivos**: `src/app/admin/pages/order-detail/order-detail.component.html`, `src/app/admin/pages/dashboard/dashboard.component.html`, `src/app/admin/pages/orders/orders.component.html`
- **Verificación**: `ng build --configuration production` OK; `ng test --watch=false` **216/216 OK**.

### 2026-08-17 — chore — Limpieza de referencias a GitHub Pages
- **Descripción**: Se eliminó `.github/workflows/deploy.yml` (ya no se necesita, Cloudflare Pages deploya automáticamente via GitHub integration). Se actualizaron README.md, docs/ARCHITECTURE.md y backend/.env.example para reflejar Cloudflare Pages como plataforma de frontend.
- **Archivos**: `.github/workflows/deploy.yml` (eliminado), `README.md`, `docs/ARCHITECTURE.md`, `backend/.env.example`

### 2026-08-17 — config — Migración de frontend de GitHub Pages a Cloudflare Pages
- **Descripción**: El frontend ahora se despliega en Cloudflare Pages en vez de GitHub Pages. Se creó `scripts/cloudflare-build.sh` que inyecta `environment.ts` y `environment.prod.ts` desde variables de entorno de Cloudflare (API_URL), eliminando la necesidad de commitear archivos de entorno. Se creó `public/_redirects` con regla SPA fallback (`/* /index.html 200`), reemplazando el truco de copiar `index.html` → `404.html`. Base href cambió de `/LCApp/` a `/`.
- **Archivos**: `scripts/cloudflare-build.sh` (nuevo), `public/_redirects` (nuevo)
- **Decisión clave**: Cloudflare Pages elegido por bandwidth ilimitado gratis (vs 100GB/mes en Vercel/Netlify), mismo ecosistema que R2, y sostenibilidad del plan gratuito (el negocio core de Cloudflare es seguridad enterprise, no hosting).

### 2026-08-17 — fix — Auth interceptor maneja 401 y redirige a login
- **Descripción**: `AuthInterceptor` ahora captura respuestas 401 de la API. Cuando un token expirado o inválido produce un 401, el interceptor limpia la sesión (`authService.logout()`) y redirige a `/login`. Antes el guard solo verificaba la presencia del token en localStorage sin validar expiración, lo que resultaba en un panel admin vacío con navegación habilitada.
- **Archivos**: `src/app/core/interceptors/auth.interceptor.ts`
- **Decisión clave**: Se agregó `catchError` al pipe del interceptor en vez de crear un interceptor separado. La lógica de logout + redirect está en el mismo interceptor que agrega el Bearer, manteniendo la separación de responsabilidades.

### 2026-08-17 — fix — openEdit() ya no borra imágenes no-primarias + limpieza de huérfanas en R2 + DELETE de producto + cleanup al cancelar form + R2 key naming descriptivo + fallback onerror en card
- **Descripción**: Múltiples mejoras en el sistema de imágenes: (1) `openEdit()` carga TODAS las imágenes del producto en `form.images[]`. (2) PUT `/api/products/:id` borra del R2 las imágenes que ya no están referenciadas. (3) Nuevo DELETE `/api/products/:id` con limpieza R2. (4) Nuevo DELETE `/api/images` para limpiar URLs específicas. (5) Al cancelar el form, se borran de R2 las imágenes que nunca se vincularon a un producto (`pendingImageUrls`). (6) Las claves R2 ahora usan el slug del producto (`products/rosa-roja-abc12/400w.webp`) en vez de UUID random. (7) `ProductCardComponent` ahora maneja `(error)` en `<img>` — si una imagen fue borrada de R2, muestra la inicial del nombre en vez de un ícono roto. `normalizeImages()` respeta `isPrimary` y `order` del cliente.
- **Archivos**: `backend/lib/r2.js`, `backend/routes/products.js`, `backend/routes/images.js`, `src/app/admin/pages/products/products.component.ts|html|scss`, `src/app/core/models/product.model.ts`, `src/app/core/services/catalog.service.ts`, `src/app/core/services/image.service.ts`, `src/app/shared/components/product-card/product-card.component.ts|html`, `src/app/admin/pages/products/products.component.spec.ts`
- **Decisión clave**: `deleteImageUrls()` usa batch `DeleteObjectsCommand` (hasta 1000 keys por lote) y es best-effort (no falla la operación principal si la limpieza falla). La limpieza de huérfanas en PUT es best-effort: calcula diff entre URLs viejas y nuevas, borra las que sobran, sin await bloqueante.

### 2026-08-17 — feat — Product card muestra imagen con srcset + README actualizado
- **Descripción**: `ProductCardComponent` ahora muestra la imagen del producto (primaria o primera disponible) con `srcset` WebP y `sizes` adaptativo, en lugar de solo la inicial del nombre. Si no hay imagen, mantiene el fallback a la letra. SCSS: `.card-media img` con `object-fit: cover`. README actualizado con Cloudflare R2, Node ≥20, env vars R2, `POST /api/images` y estructura de archivos.
- **Archivos**: `src/app/shared/components/product-card/product-card.component.ts|html|scss`, `README.md`
- **Decisión clave**: Se reutiliza la misma lógica de `srcset` de `product-gallery` (variantes WebP del backend) en la card, con `sizes="(min-width: 720px) 280px, 50vw"`.

### 2026-08-16 — feat — Self-hosting de imágenes: upload a Cloudflare R2 con variantes WebP y srcset
- **Descripción**: Los productos ahora pueden usar imágenes propias subidas desde el admin. `POST /api/images` (JWT, multer en memoria, máx. 5 MB, JPEG/PNG/WebP/AVIF/GIF) procesa la imagen con `sharp` (corrige orientación, genera **WebP 400/800/1200w** sin agrandar, quality 80) y la sube a **Cloudflare R2** como `products/<uuid>/<width>w.webp` con cache immutable, respondiendo `{ variants, primaryUrl }`; si una subida falla a mitad, borra los objetos ya creados (best-effort). `ProductImage` ganó `variants: [{ width, url }]` y `normalizeImages()` los preserva en el modelo. En el frontend: `ImageService.uploadImage(file)` (multipart; el `AuthInterceptor` pone el Bearer), el formulario de producto tiene input de archivo que sube y rellena `imageUrl` + variantes (manteniendo el campo de URL manual), y la galería usa `srcset` real (`sizes="(min-width: 720px) 520px, 100vw"`); los productos viejos sin variantes siguen funcionando sin srcset.
- **Archivos**: `backend/routes/images.js` (nuevo), `backend/lib/r2.js` (nuevo), `backend/server.js`, `backend/models/Product.js`, `backend/routes/products.js`, `backend/package.json` (+ `engines` Node ≥20.9), `backend/.env.example`, `src/app/core/models/product.model.ts`, `src/app/core/services/image.service.ts|spec.ts` (nuevos), `src/app/admin/pages/products/products.component.ts|html|scss|spec.ts`, `src/app/catalog/components/product-gallery/product-gallery.component.ts|html|spec.ts`, `docs/ARCHITECTURE.md`, `docs/adr/009-images-r2.md`
- **Decisión clave**: **Cloudflare R2** (S3-compatible, $0 egress) porque el disco de Render es efímero y no puede guardar archivos; Cloudinary descartado (no disponible en el país del usuario). El backend NO sirve archivos: sube al bucket y el frontend usa las URLs públicas de R2 (los `<img>` no requieren CORS para display). La **Public Development URL** (`pub-…r2.dev`) se activó como v1 porque el frontend está en GitHub Pages sin dominio propio; el **custom domain** queda pendiente (migración transparente: solo cambia `R2_PUBLIC_URL`, los objetos tienen nombres hasheados estables). Bucket `lcapp-images` en Standard (no Infrequent Access: acceso frecuente + mínimo de 30 días). Local pasó a Node 20.19 vía nvm (sharp 0.35 y AWS SDK v3 exigen ≥20); Render debe provisionar Node 20+ (`engines`).
- **Verificación**: `ng build --configuration production` OK; `ng test --watch=false` **216/216 OK** (+5 tests products: upload ok/mime/tamaño/error/payload; +2 galería srcset; +2 ImageService). Backend: `node --check` OK en archivos modificados y smoke del pipeline sharp (400/800/1200w → WebP válido).

### 2026-08-16 — feat — Optimización de carga de imágenes con fallback a la inicial
- **Descripción**: Las imágenes del detalle y del carrito pasan a cargarse de forma óptima con atributos nativos: la imagen principal de la galería usa `fetchpriority="high"` (es el LCP de la vista), y las miniaturas y las del carrito usan `loading="lazy"` + `decoding="async"`. Además, si una URL remota se rompe, el `<img>` dispara `(error)` y se muestra la inicial/placeholder como cuando no hay imagen (antes solo aparecía si la URL estaba vacía). En la galería el fallback es por imagen (una miniatura rota no afecta a las demás) y `selectImage()` reintenta la principal al cambiar de miniatura.
- **Archivos**: `src/app/catalog/components/product-gallery/product-gallery.component.ts|html` (+ `spec.ts` nuevo), `src/app/cart/components/cart-item/cart-item.component.ts|html` (+ `spec.ts` nuevo)
- **Decisión clave**: Se descartó `NgOptimizedImage` por ahora: las imágenes son URLs remotas arbitrarias pegadas por el admin, sin CDN/loader que redimensione, así que el `srcset` de la directiva sería inútil (inyectaría `?w=…` sobre hosts ajenos). El Tier 1 deja todo listo para sumar `srcset` real el día que se self-hosteen imágenes con variantes (upload + sharp + WebP/AVIF es feature pendiente; Cloudinary descartado por el usuario — no disponible en su país). Pendiente también: gestión multi-imagen en el formulario admin (el modelo `Product.images[]` y la galería ya lo soportan).
- **Verificación**: `ng build --configuration production` OK; `ng test --watch=false` **207/207 OK** (6 specs nuevos de galería + 5 de cart-item).

### 2026-08-16 — fix — Quitar overflow y max-height del bloque de líneas en el modal de registrar venta
- **Descripción**: El campo `.sale-lines` del modal de venta manual dejaba de tener scroll interno (`max-height: 40vh; overflow-y: auto`), de modo que las líneas de producto se muestran completas y el scroll lo resuelve el modal en sí.
- **Archivos**: `src/app/admin/pages/sales/manual-sale-modal/manual-sale-modal.component.scss`
- **Verificación**: `ng build --configuration production` OK; `ng test --watch=false` **196/196 OK**.

### 2026-08-16 — feat — Rediseño de la vista del cliente: home con cinta de categorías y grid de todos los productos
- **Descripción**: El inicio (`/`) reemplaza las tarjetas de categorías por un **hero + cinta horizontal deslizable de categorías + grid de productos**. Por defecto carga todos los productos activos con el chip "Todos" activo; seleccionar un chip filtra por categoría. La cinta es `overflow-x: auto` (scrollbar oculto), **sticky arriba** bajo el header en desktop y **fixed abajo** en móvil (≤600px, con `padding-bottom` en el contenido para que la última fila de productos no quede tapada). Los chips se estilizan como **tabs**: sin píldora ni borde indicador; la categoría seleccionada se marca con **background primary** en el botón (que ocupa toda la altura de la barra) y los tabs quedan centrados (pseudo-elementos `margin: auto`, que no rompen el scroll al desbordar) **solo en desktop**. Para señalar más contenido la cinta muestra **degradados en los bordes** (izquierdo/derecho) y **flechas ‹ ›** (visibles en **desktop** cuando hay overflow; `canScrollLeft`/`canScrollRight` calculados por `scrollLeft`/`clientWidth`/`scrollWidth` en `ngAfterViewChecked` y en el evento `scroll`; la flecha llama a `scrollBy({ left: ±320, behavior: 'smooth' })`). En **móvil** no hay flechas ni centrado, la cinta pierde el padding lateral (`padding: 0` para que los tabs lleguen de borde a borde) y los tabs usan `flex: 1 0 auto`: el ancho se ajusta al contenido y crecen para repartirse la pantalla; con muchas categorías no se encogen y la cinta desborda, dejando solo el degradado como pista de scroll. Al **seleccionar una categoría, la cinta centra el tab activo** con `scrollIntoView({ behavior: 'smooth', inline: 'center', block: 'nearest' })` — corrige el caso "seleccioné la última y queda fuera de vista" (ojo: el índice del chip en el DOM suma 1 por el tab "Todos", que se antepone a las categorías del `ngFor`). El contenedor de contenido (`.home-content`) gana `padding-bottom: $space-6` **solo en desktop** para que el grid no quede pegado al borde inferior (en móvil la cinta fija y el `padding-bottom` de `.home` ya lo resuelven, así que ahí se resetea a 0). El hero pierde los badges de "Envío gratis / Garantía / Soporte 24/7". Se extrae la tarjeta de producto a un **`ProductCardComponent` compartido** (`SharedModule`) y `ProductListComponent` queda como deep link (`/catalog/:categoryId`) reusando la misma tarjeta. Se elimina el footer de `app.component` (y el código huérfano asociado: `year`, `contact`, `getWhatsAppHref`/`getInstagramHref`/`getTelegramHref`, inyección de `ContactService`; el servicio se conserva). El **header pasa de `sticky` a `fixed`** (top 0) para que nunca desaparezca al scrollear; `main` compensa con `padding-top: 2.75rem` (sin padding en rutas admin, clase `main--no-pad`).
- **Archivos**: `src/app/shared/components/product-card/` (nuevo), `src/app/shared/shared.module.ts`, `src/app/shared/components/header/header.component.scss`, `src/app/home/pages/home/` (nuevo, reemplaza `category-list`), `src/app/home/home-routing.module.ts`, `src/app/home/home.module.ts`, `src/app/catalog/pages/product-list/product-list.component.ts|html|scss|spec.ts`, `src/app/app.component.html|ts|scss|spec.ts`
- **Decisión clave**: `ProductCardComponent` usa `OnPush` + `ChangeDetectorRef.markForCheck()` para el estado "added" (el timer de 1.5s no renderizaba con la estrategia por defecto heredada). Los chips viven en estado del componente (sin cambios de URL), manteniendo `/catalog/:categoryId` como única deep link. El backend no cambió: `GET /api/products` ya devuelve solo activos y filtra por `?categoryId`. El header es `fixed` porque el `sticky` es frágil ante ancestros con `overflow` (p. ej. `body.modal-lock`) y el requisito es que no desaparezca nunca.
- **Verificación**: `ng build --configuration production` OK; `ng test --watch=false` **193/193 OK** (+product-card spec, +home spec; product-list spec reescrito y autocontenido; eliminado category-list spec).

### 2026-08-15 — fix — El modal de registrar venta no vuelve a abrirse al recargar la página
- **Descripción**: Al cerrar el modal (Cancelar) o tras registrar una venta, `?registrar=1` quedaba en la URL; al recargar la página el componente lo detectaba y volvía a abrir el modal. Ahora `closeRegister()` limpia el query param con `router.navigate([], { queryParams: { registrar: null }, queryParamsHandling: 'merge', replaceUrl: true })`, que quita solo `registrar` conservando el resto de la URL (y `replaceUrl` evita ensuciar el historial).
- **Archivos**: `src/app/admin/pages/sales/sales.component.ts|spec.ts`
- **Decisión clave**: El query param era la única fuente del estado "abrir modal"; al cerrar, la URL debe volver a su estado base. El `queryParamMap` emite de nuevo con `registrar=null` y la guardia `params.get('registrar') === '1'` evita re-abrir.
- **Verificación**: `ng test --watch=false` **179/179 OK** (+1 test: "quita ?registrar=1 de la URL al cerrar el modal").

### 2026-08-15 — style — Modal registrar venta: el error se muestra en un diálogo (como la confirmación) + tests de Orders autocontenidos
- **Descripción**: El error del modal de ventas manuales ya no se muestra como texto en el formulario sino en un **diálogo modal** (`AppConfirmDialogComponent` con `variant="cancel"`, título "No se pudo registrar la venta", botones "Revisar" / "Entendido" — ambos cierran el diálogo y devuelven al formulario). Aplica a validación de línea, error del servicio al guardar y error al cargar productos. De paso se corrigieron 2 tests de `orders.component.spec.ts` que dependían de estado de otro test: no llamaban a `createComponent()` y Jasmine corre los specs en orden aleatorio, por lo que fallaban intermitentemente con `TypeError: Cannot set properties of undefined (setting 'totalPages')`.
- **Archivos**: `src/app/admin/pages/sales/manual-sale-modal/manual-sale-modal.component.ts|html|scss|spec.ts`, `src/app/admin/pages/orders/orders.component.spec.ts`
- **Decisión clave**: Reutilizar el patrón de confirmación existente (`formError` conserva el mensaje; `showError` controla la visibilidad del diálogo) en lugar de un inline que competía con los botones de acción. En tests: **cada spec debe ser autocontenido** (crear su propio componente); nunca depender de que otro test deje estado.
- **Verificación**: `ng test --watch=false` **178/178 OK** en 2 corridas consecutivas (la flakiness de Orders desapareció).

### 2026-08-15 — fix — Backend: validar stock agregado por producto en ventas manuales (evita sobre-venta)
- **Descripción**: `POST /api/orders/manual` validaba cada línea contra el stock actual por separado y descontaba por línea, por lo que el mismo producto en varias líneas podía sobre-vender (ej: stock 5 con líneas 4+4 → ambas pasaban, stock quedaba -3). Ahora se **acumulan las cantidades por producto** en un `Map` (dentro del mismo loop, sin queries extra) y se valida el total por producto antes de crear la orden y descontar. Se permite repetir el producto en varias líneas con precios distintos (caso mostrador).
- **Archivos**: `backend/routes/orders.js` (POST /manual), `docs/ARCHITECTURE.md`
- **Decisión clave**: La validación unitaria por línea es insuficiente cuando el mismo producto aparece en más de una línea; la acumulación por `productId` convierte el chequeo en "stock del producto vs suma de cantidades". El `Map` guarda `{ name, stock, quantity }` para reusar el fetch ya hecho y evitar consultas duplicadas.
- **Verificación**: Smoke test con instancia temporal (puerto 3100): producto temp stock 5 → líneas 4+4 = **400** `disponible 5, solicitado 8` (stock intacto); líneas 2+2 (precios 100 y 50) = **201**, total 300, 2 líneas separadas, stock 5→1. Cleanup de datos temp OK. Frontend no cambió (178/178 sigue OK).

### 2026-08-15 — style — Modal registrar venta: en móvil Cantidad angosta, Precio y Quitar en la misma fila
- **Descripción**: En pantallas ≤600px la línea de producto pasó de `1fr 1fr` (cantidad y precio a la mitad del ancho cada uno, Quitar abajo) a `90px 1fr auto` con grid-areas: **Producto** a ancho completo arriba, y debajo **Cantidad** (columna angosta de 90px, ya no ocupa la mitad), **Precio unitario** (flexible) y **Quitar** al lado del precio en la misma fila. Sin producto seleccionado, la línea queda en 1 columna con el selector full width y Quitar abajo a la izquierda.
- **Archivos**: `src/app/admin/pages/sales/manual-sale-modal/manual-sale-modal.component.scss`
- **Decisión clave**: Con la clase `sale-line--has-product` se alterna la plantilla del grid en móvil; `grid-template-areas` fija las zonas (`product`/`qty`/`price`/`remove`) sin depender del orden de los hijos. De paso se corrigió que `.sale-lines__add` (margin-top del botón "+ Agregar producto") quedó anidado por accidente dentro del media query en el edit previo; ahora vive a nivel de `.manual-sale-form`.
- **Verificación**: `ng test --watch=false` **178/178 OK**.

### 2026-08-15 — style — Modal registrar venta: cantidad y precio unitario aparecen recién al seleccionar el producto
- **Descripción**: Cada línea del modal de ventas manuales ahora muestra solo el selector de producto hasta que se elige uno; al seleccionarlo aparecen **Cantidad** y **Precio unitario** (con el precio efectivo autocompletado y editable, como antes). El grid de la línea se adapta: sin producto el selector ocupa todo el ancho (`minmax(0, 1fr) auto`), con producto vuelve a las 4 columnas (`minmax(0, 1fr) 90px 110px auto`) vía clase `sale-line--has-product`. En móvil mantiene el apilado actual.
- **Archivos**: `src/app/admin/pages/sales/manual-sale-modal/manual-sale-modal.component.html|scss|spec.ts`
- **Decisión clave**: `*ngIf="line.productId"` oculta los campos hasta que hay producto; el `ngFor` con `trackBy: index` + clase condicional cambia la plantilla del grid. Los inputs conservan su `name` como propiedad (no atributo DOM), por eso el test selecciona por clase (`.sale-line__qty`), no por `[name]`.
- **Verificación**: `ng test --watch=false` **178/178 OK** (+1 test). El selector `input[name="qty-0"]` no matchea porque Angular renderiza `name` como propiedad, no atributo — se usa el selector por clase.

### 2026-08-15 — style — Modal registrar venta: "+ Agregar producto" fuera del área de scroll
- **Descripción**: El botón "+ Agregar producto" estaba dentro de `.sale-lines` y desaparecía al scrollear la lista. Se movió fuera del `fieldset` (clase nueva `sale-lines__add` con `margin-top`): queda fijo debajo de la lista de productos, siempre visible junto a Total y los botones de acción.
- **Archivos**: `src/app/admin/pages/sales/manual-sale-modal/manual-sale-modal.component.html|scss`
- **Verificación**: `ng test --watch=false` **177/177 OK**.

### 2026-08-15 — style — Modal registrar venta: scroll interno solo en la vista de productos (max-height 40vh)
- **Descripción**: En vez de que scrollee todo el cuerpo del modal, ahora solo la lista de productos (`.sale-lines`) scrollea con `max-height: 40vh` + `overflow-y: auto`. Cliente/Teléfono, Fecha, Total y los botones Cancelar/Registrar quedan siempre visibles y fijos; el botón "+ Agregar producto" vive al final del scroll de la lista.
- **Archivos**: `src/app/admin/pages/sales/manual-sale-modal/manual-sale-modal.component.scss`
- **Decisión clave**: Preferencia del usuario: el scroll va en la zona que crece (las líneas), no en todo el formulario. La protección global del panel compartido (max-height + scroll del body) queda como red de seguridad para modales sin esta zona scrollable.
- **Verificación**: `ng test --watch=false` **177/177 OK**.

### 2026-08-15 — style — Modal registrar venta: Cliente/Teléfono en columna en pantallas chicas (breakpoint alineado a 600px)
- **Descripción**: En pantallas chicas el par Cliente/Teléfono seguía en fila mientras el resto del modal ya estaba en modo móvil. Causa: `.form-row` (`_forms.scss`) solo colapsa a 1 columna en `max-width: 480px`, pero el modal usa su propio breakpoint móvil en **600px** (líneas de producto). Fix: override en el scss del modal que pone `.form-row` en 1 columna al mismo breakpoint de 600px, alineando todo el formulario.
- **Archivos**: `src/app/admin/pages/sales/manual-sale-modal/manual-sale-modal.component.scss`
- **Decisión clave**: El breakpoint móvil del modal es 600px; `_forms.scss` (genérico, 480px) no aplica a la ventana 481-600px. El override local unifica el comportamiento del modal.
- **Verificación**: `ng test --watch=false` **177/177 OK**.

### 2026-08-15 — style — Modal registrar venta: línea de producto desbordaba en pantallas grandes (botón "Quitar" oculto)
- **Descripción**: En pantallas grandes la línea de producto del modal de registrar venta desbordaba el panel de 620px y el botón "Quitar" (última columna del grid `auto`) quedaba fuera de vista. Causa: la columna `1fr` equivale a `minmax(auto, 1fr)` y no puede encogerse por debajo del ancho mínimo del `<select>` (= la opción más larga, "Producto — $precio · stock: N"); con nombres largos el grid total superaba el panel y empujaba el botón. Fix: `grid-template-columns: minmax(0, 1fr) 90px 110px auto` (la columna del producto sí puede encogerse y el select trunca) + `width: 100%; min-width: 0` en `select`/`input` de la línea y `min-width: 0` en los items del grid.
- **Archivos**: `src/app/admin/pages/sales/manual-sale-modal/manual-sale-modal.component.scss`
- **Decisión clave**: `minmax(0, 1fr)` elimina el piso de min-content de la columna elástica; es el patrón para grids con contenido no comprimible (selects/inputs largos).
- **Verificación**: `ng test --watch=false` **177/177 OK**.

### 2026-08-15 — style — Modal compartido: altura máxima con scroll interno del cuerpo
- **Descripción**: `AppModalComponent` (usado por productos, categorías, ventas manuales y el diálogo de confirmación) solo limitaba el ancho; con formularios largos (p. ej. varias líneas de producto en el modal de registrar venta) el panel podía superar la altura del viewport y los botones quedaban fuera de alcance sin scroll. Se agrega `max-height: calc(100vh - 2 * $space-4)` al `.modal-panel` (flex column) y `overflow-y: auto` al `.modal-body` (con `min-height: 0` para que flex permita el scroll).
- **Archivos**: `src/app/shared/components/modal/app-modal.component.scss`
- **Decisión clave**: El arreglo va en el componente **compartido** (no solo en el modal de ventas manuales) porque el problema es general de cualquier modal alto; el cuerpo scrollea y el header (título + X) queda fijo.
- **Verificación**: `ng test --watch=false` **177/177 OK** y `ng build --configuration production` OK.

### 2026-08-15 — feat — Admin: registro de ventas manuales (fuera de la página) con modal y descuento de stock
- **Descripción**: El admin puede registrar ventas que se concretan **por fuera de la web** (mostrador, conocidos, WhatsApp) para que el stock y el resumen de `/admin/ventas` reflejen la realidad. Nuevo endpoint `POST /api/orders/manual` (JWT): crea la orden directo `confirmed` con `source: 'manual'`, código `MAN-XXXXX` (nuevo campo `source: 'web' | 'manual'` en `Order`; las web siguen `CAR-XXXXX`), **descuenta stock al registrar** y guarda `createdAt = confirmedAt = saleDate` (default hoy, validada para no ser futura) para que caiga en la ventana correcta del resumen. Body `{ customerName?, customerPhone?, saleDate?, items: [{ productId, quantity, price? }] }`: precio por línea default = precio efectivo (`discountPrice ?? price`) y editable; valida producto activo, stock disponible, qty entero ≥ 1, precio ≥ 0 y fecha no futura. `customerName` opcional → "Cliente de mostrador". Frontend: nuevo `ManualSaleModalComponent` dentro de `/admin/ventas` con botón rosa "+ Registrar venta" en el header y link "+ Registrar venta" en el sidebar (color rosa distintivo) que navega a `/admin/ventas?registrar=1` y abre el mismo modal; formulario con cliente/teléfono opcionales, fecha (max hoy), líneas (select de productos activos, cantidad topeada al stock, precio editable, Quitar/+Agregar, total) y **doble confirmación** (reusa `AppConfirmDialogComponent`) antes de guardar → snackbar `Venta MAN-XXXXX registrada` y recarga del resumen. Badge "Manual" junto al código en la lista de órdenes y el detalle.
- **Archivos**: `backend/routes/orders.js`, `backend/models/Order.js`, `src/app/core/models/order.model.ts`, `src/app/core/services/order.service.ts`, `src/app/core/services/order.service.spec.ts`, `src/app/admin/pages/sales/manual-sale-modal/manual-sale-modal.component.ts|html|scss|spec.ts` (nuevos), `src/app/admin/pages/sales/sales.component.ts|html|scss|spec.ts`, `src/app/admin/styles/_tables.scss`, `src/app/admin/pages/orders/orders.component.html|spec.ts`, `src/app/admin/pages/order-detail/order-detail.component.html`, `src/app/admin/layout/admin-layout.component.html|scss`, `src/app/admin/admin.module.ts`, `docs/ARCHITECTURE.md`, `docs/adr/008-ventas-manuales.md` (nuevo)
- **Decisión clave**: Las ventas manuales nacen `confirmed` + `source: 'manual'` → cuentan automáticamente en `stats`, `summary` y la lista sin tocar las agregaciones. El stock se descuenta **al registrar** (no al confirmar) porque la mercancía ya salió del negocio; por eso hay doble confirmación. `createdAt = saleDate` (fecha editable, no futura) para que el resumen calendario quede correcto. Prefijo `MAN-` para distinguir ventas externas de un vistazo. El modal se abre por query param `registrar=1` (no hay ruta extra).
- **Verificación**: `ng test --watch=false` **177/177 OK** (+19) y `ng build --configuration production` OK. Smoke test backend en `PORT=3100`: `POST /api/orders/manual` → `MAN-9IAOJ` `confirmed`, `source=manual`, `total=140` (precio sobreescrito 2×70), stock Teclado 25→23, aparece en `GET /api/orders?q=` con badge manual y en el resumen WEEK (sales 4→5, revenue 463.45→603.45, teclado 1→3 unidades); validaciones OK (fecha futura, stock insuficiente, items vacíos → 400 con mensaje claro). Instancia temporal detenida.

### 2026-08-14 — feat — Admin: página de resumen de ventas por período (diario, semanal, mensual)
- **Descripción**: Nueva página `/admin/ventas` ("Ventas" en el sidebar) con toggle **Diario / Semanal / Mensual** (días calendario: hoy desde las 00:00, lunes a hoy, día 1 del mes a hoy — zona horaria del servidor). Muestra 7 tarjetas métricas (Ventas confirmadas, Ingresos, Unidades vendidas, Ticket promedio, Canceladas, Tasa de cancelación %, Pendientes en el período) + tabla **Productos más vendidos** (ordenable por Unidades o Ingreso vía headers clickeables) + tabla **Por categoría** (ordenada por ingreso). Backend: nuevo endpoint autenticado `GET /api/orders/summary?range=day|week|month` que agrega en paralelo (counts por estado en la ventana + `$unwind` de items para ingreso/unidades + top 20 productos + desglose por categoría con 2 `$lookup` products→categories; categoría ausente → "Sin categoría"). `avgTicket` y `cancellationRate` se derivan en el frontend (testeables en specs). `seed.js` ahora genera **12 órdenes demo** con `createdAt` repartidos en hoy/esta semana/este mes (auxiliares de fecha relativos: `startOfWeek`/`startOfMonth`/`beforeWeekStart`) para poder probar los 3 rangos con números reales.
- **Archivos**: `backend/routes/orders.js`, `backend/seed.js`, `src/app/core/models/order.model.ts`, `src/app/core/services/order.service.ts`, `src/app/core/services/order.service.spec.ts` (nuevo), `src/app/admin/pages/sales/sales.component.ts|html|scss|spec.ts` (nuevos), `src/app/admin/styles/_metrics.scss` (nuevo), `src/app/admin/styles/admin.scss`, `src/app/admin/pages/dashboard/dashboard.component.scss`, `src/app/admin/admin-routing.module.ts`, `src/app/admin/admin.module.ts`, `src/app/admin/layout/admin-layout.component.html`, `docs/ARCHITECTURE.md`, `docs/adr/007-resumen-ventas-periodo.md` (nuevo)
- **Decisión clave**: Ventanas **calendario** (no rodantes) porque el admin piensa en "ventas de hoy/semana/mes". Los items de la orden ya snapshotean nombre y precio → las agregaciones no dependen del catálogo actual (precios históricos correctos); solo el desglose por categoría hace lookup (categorías no se borran, solo se desactivan). Frontera de datos respetada: el frontend recibe un solo DTO (`OrderSummary`) ya agregado. TTL lazy del backend aplica antes del resumen (una pending >48 h cuenta como cancelada — comportamiento existente, no un cambio). Fix de estilos: `.metrics`/`.metric` estaban scoped en `dashboard.component.scss` y no aplicaban a Ventas (se veían como texto plano) → se extrajeron al partial global `admin/styles/_metrics.scss` (mismo patrón que `_tables`/`_forms`, cargado una sola vez vía `admin.scss` en `angular.json`) y se eliminó el bloque duplicado del dashboard.
- **Verificación**: Smoke test backend en `PORT=3100` contra el seed: DAY sales=1, revenue=149.97, units=3; WEEK sales=4, revenue=463.45, units=9; MONTH sales=7, revenue=913.38, units=18, categorías correctas; default `week`. `ng test` **158/158 OK** (+9) y `ng build --configuration production` OK.


### 2026-08-14 — feat — Admin: resaltado y filtro de stock bajo/agotado en la lista de productos
- **Descripción**: En `/admin/products` ahora se nota de un vistazo el estado de stock **sin badges**: las filas de productos agotados (`stock === 0`) se tiñen con un fondo rojo suave (`rgba($color-danger, 0.08)`) y las de stock bajo (`stock <= 10`) con ámbar suave (`rgba($color-warning, 0.1)`); el nombre de esos productos se muestra en **cursiva** y, para los **agotados**, el nombre y el **número de stock** van en **rojo** (`$color-danger`, semibold/bold) — un color fuera de la paleta del diseño que genera ruido a propósito para que salte a la vista. Encima de la tabla hay una barra de resumen con dos chips clickeables: **Agotados (N)** y **Stock bajo (N)** (solo cuentan productos **activos**; los inactivos se omiten porque no son vendibles y ya muestran el badge "Inactivo"). Al hacer click en un chip la tabla se **filtra** mostrando solo esos productos (chip activo resaltado con `aria-pressed`); click de nuevo o botón "Ver todas" resetea. Mensaje de vacío contextual según el filtro ("No hay productos agotados."). El umbral se unificó: nueva constante `LOW_STOCK_THRESHOLD = 10` en `src/app/core/models/product.model.ts` (concepto de dominio, única fuente de verdad) y se refactorizaron los 2 hardcodeos del catálogo (`product-list.getStockStatus`, `product-detail.getStockStatus`). Token nuevo `$color-warning-hover`.
- **Archivos**: `src/app/core/models/product.model.ts`, `src/app/catalog/pages/product-list/product-list.component.ts`, `src/app/catalog/pages/product-detail/product-detail.component.ts`, `src/app/admin/pages/products/products.component.ts|html|scss|spec.ts`, `src/styles/_variables.scss`, `docs/ARCHITECTURE.md`
- **Decisión clave**: En vez de badges, **color de fila + cursiva** (lo que el usuario pidió) para no competir con el badge de Estado; el **rojo en nombre y número de stock de los agotados es un ruido visual intencional** (color fuera de la paleta) para prioridad máxima. Los chips de conteo funcionan como filtro (no hay `<select>` aparte): un solo mecanismo, menos UI. El resaltado y los conteos solo consideran productos **activos** — el admin debe reponer lo que se puede vender; un inactivo ya comunica su estado con el badge. El filtro es 100% cliente (la lista ya se carga completa vía `getAllProducts`); getters derivados de `products` → se actualizan solos tras guardar. Umbral centralizado en el modelo de dominio para que catálogo y admin nunca diverjan.
- **Verificación**: `ng test --watch=false` **149/149 OK** (+7: clasificación de stock por rango e inactivos, conteos solo activos, filtro y reset, clases `.row-out`/`.row-low` + cursiva en el DOM, nombre en rojo solo para agotados, click en chip filtra y "Ver todas" resetea, vacío contextual). `ng build --configuration production` OK sin warnings de budget.


### 2026-08-14 — feat — Reconfirmación en admin al confirmar/cancelar órdenes y verificación del estado al contactar en el carrito
- **Descripción**: Dos arreglos alrededor del ciclo de vida de la orden. **(1) Admin**: cada botón Confirmar/Cancelar (lista de órdenes, detalle y dashboard) ahora abre un diálogo de confirmación antes de llamar al servicio. Nuevo componente compartido `AppConfirmDialogComponent` en `SharedModule` (envuelve `AppModalComponent`): inputs `open/title/message/confirmLabel/cancelLabel/variant ('confirm'|'cancel')`, outputs `confirmed/cancelled`, dismissible por backdrop/Esc → `cancelled`. Las tres páginas expusieron `pendingAction` (confirm/cancel + orden o solo tipo), `requestConfirm`/`requestCancel` (abren el diálogo), `runPendingAction` (ejecuta recién al aceptar) y `closePendingAction`; los métodos `confirmOrder`/`cancelOrder` originales quedan intactos. **(2) Carrito**: `CartSummaryComponent.openContact` ahora re-verifica el estado de la orden registrada **al momento de contactar** (`verifyThenContinue`), porque antes solo se verificaba en `ngOnInit` y una orden confirmada por el admin mostraba el modal de redirect en vez del de agradecimiento. Nuevo flujo: `confirmed` → modal "¡Gracias por tu compra!" (sin redirect, vuelve al inicio a los 4s); `cancelled` → aviso "Pedido cancelado" y limpia el código; `pending` → flujo actual (sync si el carrito fue modificado, si no redirect); 404 → aviso "Pedido no encontrado"; error de red → degrada y redirige igual (no bloquear el contacto). Guard de doble-click incluye `checkingOrder`. Tokens SCSS nuevos: `$color-success-hover`, `$color-danger-hover` (sin `darken()` deprecado).
- **Archivos**: `src/app/shared/components/confirm-dialog/app-confirm-dialog.component.ts|html|scss|spec.ts` (nuevos), `src/app/shared/shared.module.ts`, `src/app/admin/pages/orders/orders.component.ts|html|spec.ts`, `src/app/admin/pages/order-detail/order-detail.component.ts|html|spec.ts`, `src/app/admin/pages/dashboard/dashboard.component.ts|html|spec.ts`, `src/app/cart/components/cart-summary/cart-summary.component.ts|spec.ts`, `src/styles/_variables.scss`, `docs/ARCHITECTURE.md`
- **Decisión clave**: La verificación en el carrito corre **solo al click de contacto** (no polling): el admin verifica cuando contacta, así que en ese momento se consulta el estado y se decide el modal. El diálogo de admin es un componente compartido (wrapper del modal existente) para no duplicar HTML/estilos en las 3 páginas; la reconfirmación es UI pura (no toca la API ni la lógica de `confirmOrder`/`cancelOrder`). Error de red en la verificación → fallback degradado a redirect (mejor contactar aunque el estado sea viejo que bloquear la venta).
- **Verificación**: `ng test --watch=false` **142/142 OK** (+19: 6 del diálogo, 4 en cart-summary para el flujo confirmada/cancelada/red/404 al contactar, 3 por página admin para request/run/close del diálogo; los tests que llamaban `confirmOrder`/`cancelOrder` directo siguen pasando porque las firmas no cambiaron). `ng build --configuration production` OK sin warnings de budget.


### 2026-08-14 — feat — Manejo de stock: agotados y últimas unidades en catálogo y carrito
- **Descripción**: Se cierra la brecha de stock detectada en la lista de productos y el carrito. **Lista de productos** (`/catalog`): cada card muestra un badge de stock ("En stock" / "¡Últimas X unidades!" / "Agotado") y el botón "Agregar al carrito" queda **deshabilitado** (con texto "Agotado") cuando `stock === 0`; `addToCart` también se protege en el componente. **CartService**: `addItem` no agrega productos agotados y **topea la cantidad al stock disponible** (también al acumular); `updateQuantity` limita al stock y **elimina el item si el producto quedó sin stock**. **CartItem**: el botón "+" se deshabilita al llegar al máximo disponible y muestra hint "Stock disponible: X". **Backend**: `POST /api/orders` y `PATCH /api/orders/:code/items` ahora validan disponibilidad (producto existe, está activo y `stock >= cantidad` solicitada) con helper `validateStockAvailability` → 400 con mensaje claro, además de la validación existente al confirmar (`PATCH /:id/confirm`, que descuenta). Tokens SCSS: se agrega `$color-warning` y se reemplazan los colores hardcodeados del badge de stock en `product-detail` (`$color-success`/`$color-warning`).
- **Archivos**: `src/app/catalog/pages/product-list/product-list.component.ts|html|scss|spec.ts`, `src/app/catalog/pages/product-detail/product-detail.component.scss`, `src/app/core/services/cart.service.ts`, `src/app/core/services/cart.service.spec.ts`, `src/app/cart/components/cart-item/cart-item.component.ts|html|scss`, `backend/routes/orders.js`, `src/styles/_variables.scss`, `docs/ARCHITECTURE.md`
- **Decisión clave**: El servidor es la única fuente de verdad del stock: se valida en alta de orden y al actualizar items (además del descuento atómico al confirmar), y el frontend **topea la cantidad** como UX para que el cliente nunca pueda pedir más de lo disponible (defensa en profundidad: si la UI falla, la API rechaza). Umbral de "pocas unidades" = `stock <= 10` (constante del frontend, no configurable). Un producto con `stock` reducido a 0 mientras está en un carrito se **elimina** del carrito en la siguiente operación de cantidad.
- **Verificación**: `ng test --watch=false` **123/123 OK** (+6 specs de stock en CartService: agotado no se agrega, tope al agregar, tope al acumular, límite en updateQuantity, item eliminado si queda sin stock; +2 en product-list: agotado no agrega ni muestra snackbar, clasificación de estado). `ng build --configuration production` OK sin warnings. Backend smoke test en instancia temporal (puerto 3100, datos reales limpios): POST con cantidad > stock → **400** "Stock insuficiente para 'Teclado mecánico': disponible 19, solicitado 118"; POST correcto → 201; PATCH items sobre pedido pending: insuficiente → 400 y correcto → 200; POST con `productId` inexistente → 400 "Producto no disponible". Órdenes de prueba eliminadas.


### 2026-08-14 — feat — Admin: alta y edición de productos y categorías
- **Descripción**: El admin deja de ser read-only para catálogo. En **Productos** (`/admin/products`): la lista ahora incluye inactivos (columna Estado + Acciones Editar/Desactivar), hay botón "Nuevo producto" y edición vía modal (`AppModalComponent` con nuevo tamaño `md`). Formulario: nombre*, categoría* (select), precio*, precio de oferta, stock, SKU, descripción, etiquetas (coma), URL de imagen opcional y checkbox activo; valida obligatorios y `discountPrice < price`. **Nueva página Categorías** (`/admin/categories`, link en sidebar): lista + alta/edición en modal (nombre*, descripción, URL imagen). Backend: `POST /api/products` y `PUT /api/products/:id` (auth) con validaciones (nombre, categoría existente, precio>0, oferta<precio, stock entero ≥0, SKU único) y **slug autogenerado** desde el nombre (sin acentos, único con sufijo `-2`, `-3`…); `POST /api/categories` y `PUT /api/categories/:id` (auth) análogos; `GET /api/products?all=true` devuelve también inactivos solo con token válido (`authenticateOptional`), el público sigue sin cambios. `CatalogService` suma `getAllProducts/createProduct/updateProduct/createCategory/updateCategory`; `handleError` ahora expone el mensaje del servidor (p. ej. "El SKU ya existe"). Estilos compartidos de tabla/formulario globalizados: `admin/styles/_tables.scss` + nuevo `_forms.scss` (`.page-header`, `.btn-primary`, `.btn-link`, `.entity-form`…) se cargan **una sola vez** vía `admin.scss` en `angular.json` y se quitaron los `@use` por componente → se elimina la duplicación que superaba el budget de 3 kB por componente.
- **Archivos**: `backend/routes/products.js`, `backend/routes/categories.js`, `backend/middleware/auth.js`, `backend/utils/slugify.js` (nuevo), `src/app/core/services/catalog.service.ts`, `src/app/core/models/product.model.ts`, `src/app/admin/pages/products/products.component.ts|html|scss|spec.ts`, `src/app/admin/pages/categories/categories.component.ts|html|scss|spec.ts` (nuevos), `src/app/admin/admin-routing.module.ts`, `src/app/admin/admin.module.ts`, `src/app/admin/layout/admin-layout.component.html`, `src/app/admin/styles/_forms.scss` (nuevo), `src/app/admin/styles/_tables.scss`, `src/app/admin/styles/admin.scss` (nuevo), `src/app/admin/pages/{orders,order-detail,dashboard}/*.scss`, `src/app/shared/components/modal/app-modal.component.ts|html|scss|spec.ts`, `angular.json`, `docs/ARCHITECTURE.md`
- **Decisión clave**: El slug lo genera el **backend** (única fuente de verdad, garantiza unicidad atómica con `-2`, `-3`…), el frontend nunca manda slug. Mutaciones de productos/categorías protegidas con `authenticate` (JWT); `GET ?all=true` usa `authenticateOptional` para no romper el acceso público. Sin `DELETE`: alta/baja vía `isActive` (las órdenes referencian `productId`/`categoryId`, no se dejan huérfanos). El formulario de producto mapea la URL única a `images[{url, alt, isPrimary, order}]` (1 imagen por producto en esta iteración; en edición se reemplaza). `AppModalComponent` gana `@Input size: 'sm'|'md'` (620px) para el formulario.
- **Verificación**: `ng test --watch=false` **116/116 OK** (productos pasa de 4 a 11 specs + 8 nuevos de categorías + 1 de tamaño md del modal); `ng build --configuration production` OK **sin warnings de budget** (antes products/categories superaban 3 kB por la duplicación de partials). Backend smoke test contra el server local: POST sin token → 401; validaciones → 400; POST → 201 con slug autogenerado (`smoke-test-lc`, duplicado → `-2`); SKU duplicado → 400; PUT parcial → 200 y regenera slug al cambiar nombre; `?all=true` con token lista todos (sin token solo activos). Datos de prueba limpiados.


- **Descripción**: El toggle del sidebar (hamburguesa/✕) dejó de flotar como caja suelta sobre el contenido (`position: absolute; right: -2.75rem` dentro del sidebar, que con `translateX(-100%)` además dejaba el menú **inaccesible** en móvil al estar el botón fuera de pantalla). Ahora en móvil hay un `<header class="admin-topbar">` fijo (solo <768px) con el botón hamburguesa + marca "LC · Admin", con los colores del sidebar (`$color-footer-bg`/`$color-pink-light`) para que se vea parte del nav. Capas: topbar `z-index:54` < backdrop `53` < sidebar `55`. Como el sidebar cubre el lado izquierdo de la topbar al abrir (y con ello el toggle), la **X de cierre vive en el propio sidebar** (`.sidebar-close`, extremo derecho del brand, solo móvil); el toggle de la topbar queda solo como apertura. El contenido compensa la barra fija con `padding-top: calc(3.5rem + space-4)` en móvil → nada queda tapado.
- **Archivos**: `src/app/admin/layout/admin-layout.component.html`, `src/app/admin/layout/admin-layout.component.scss`
- **Decisión clave**: En lugar de arreglar la posición del botón suelto, se integró en una barra superior real: visible siempre (el menú siempre es abrible en móvil, se corrige el bug de accesibilidad), no tapa contenido y visualmente pertenece al nav. La X de cierre se colocó en el sidebar (no en la topbar) porque el drawer (55) tapa el lado izquierdo de la topbar (54) al abrir.
- **Verificación**: `ng build --configuration production` OK; `ng test --watch=false` 100/100 OK (sin spec del layout, suite completa verde).

### 2026-08-13 — feat — Spinner de carga reutilizable en toda la app
- **Descripción**: Nuevo componente compartido `AppLoadingSpinnerComponent` (`app-loading-spinner`, en `SharedModule`) que reemplaza los 9 estados de carga textuales de toda la app por un anillo animado SCSS puro. Acepta `label?: string` y `size?: 'sm'|'md'|'lg'` (default `md`). Accesible: contenedor con `role="status"` + `aria-live="polite"`, anillo `aria-hidden`. Migrados: admin (dashboard, orders, order-detail, products, contact-settings), catálogo (product-detail, product-list), carrito (cart-view) y home (category-list). Se agregó `SharedModule` a `admin.module.ts` y `home.module.ts` (no lo importaban) y se limpiaron los estilos `.loading` huérfanos de `_tables.scss` y `contact-settings.component.scss`. En las páginas admin el loading usa un **estado div** (`.loading-region` en `_tables.scss`): ocupa la misma región que el contenido (min-height 320px, centrado) en vez de quedar pegado arriba — el dashboard envuelve métricas + pendientes en `*ngIf="!loading"` y muestra un único estado div de carga.
- **Archivos**: `src/app/shared/components/loading-spinner/loading-spinner.component.ts|html|scss|spec.ts` (nuevos), `src/app/shared/shared.module.ts`, `src/app/admin/admin.module.ts`, `src/app/admin/pages/{dashboard,orders,order-detail,products,contact-settings}/*.html`, `src/app/admin/styles/_tables.scss`, `src/app/admin/pages/contact-settings/contact-settings.component.scss`, `src/app/catalog/pages/{product-detail,product-list}/*.html`, `src/app/cart/pages/cart-view/cart-view.component.html`, `src/app/home/home.module.ts`, `src/app/home/pages/category-list/category-list.component.html`, `docs/ARCHITECTURE.md`
- **Decisión clave**: Anillo con `@keyframes spin` (borde de `$color-border` con `border-top-color: $color-primary`) en vez de librerías o SVG; tokens del design system ya existentes. Como pieza presentacional se controla por `@Input` (igual que el modal, distinto al snackbar que es por servicio). El host del componente es `display: flex; justify-content: center` con `grid-column: 1 / -1`: sin esto, en contextos de grid (`product-list`, `product-detail`) el spinner es un grid item que ocupa una sola celda y queda pegado a la izquierda; con el span abarca todas las columnas y se centra (inocuo en contenedores block). En specs con OnPush, los `@Input` deben setearse **antes** del primer `detectChanges` (mutar después no re-checkea la vista).
- **Verificación**: `ng test --watch=false` 100/100 OK (4 specs nuevos del spinner: anillo+label, `role=status`/`aria-live`, sin label, clases de tamaño); `ng build --configuration production` OK sin warnings de budget.

### 2026-08-13 — feat — Admin: buscador de órdenes por código con debounce
- **Descripción**: El listado de órdenes ahora tiene un campo de búsqueda por código. `GET /api/orders` acepta `?q=` y filtra por `code` con regex case-insensitive (caracteres escapados para uso literal); el `OrderService.getOrders(page, limit, status?, q?)` pasa el término y el componente lo envía con **debounce de 300ms** (resetea a la página 1 y convive con el filtro de estado). Al vaciar el buscador se recarga el listado completo; el mensaje de "no hay órdenes" distingue estado/código. Fix posterior en el mismo cambio: `onSearchInput` recibía un `event` pero `(ngModelChange)` emite el **valor** (string) — ahora recibe el string directo.
- **Archivos**: `backend/routes/orders.js`, `src/app/core/services/order.service.ts`, `src/app/admin/pages/orders/orders.component.ts|html|scss|spec.ts`, `docs/ARCHITECTURE.md`
- **Decisión clave**: Búsqueda **server-side** (no filtrar en cliente) porque la paginación ya es server-side: el `q` se combina con `page/limit/status` en el mismo query. Debounce de 300ms para no disparar un request por tecla; `distinctUntilChanged` evita recargas por valores repetidos. Los caracteres del término se escapan (regex literal) para que un código con `$`/`.` no rompa la consulta.
- **Verificación**: `node --check` OK; `ng test --watch=false` 96/96 OK (5 specs de búsqueda: 4 de unidad con `fakeAsync`/`tick` + 1 contra el DOM real escribiendo en el input, que reproduce el bug del valor vs. evento); `ng build --configuration production` OK sin warnings de budget.

### 2026-08-13 — feat — Admin: detalle de orden por código con breadcrumb y botón volver
- **Descripción**: Desde `/admin/orders`, cada fila es clicable y el código pasó a ser un enlace accesible para abrir `/admin/orders/detail/:code`. El nuevo `OrderDetailComponent` muestra el breadcrumb "Órdenes › CÓDIGO" (con "Órdenes" como link) + botón "‹ Volver a Órdenes" (ambos siempre visibles, responsive), badge de estado, fechas de creación y confirmación, nombre y teléfono del cliente (`-` si no existen), tabla de items con precio unitario, cantidad y subtotal por línea, total destacado, y Confirmar/Cancelar si la orden está `pending`. Estados loading / error / 404 "Orden no encontrada". No hubo cambios de backend: se reutiliza `GET /api/orders/:code` (admin) existente vía `OrderService.getOrderByCode(code)`.
- **Archivos**: `src/app/admin/pages/order-detail/order-detail.component.ts|html|scss|spec.ts` (nuevos), `src/app/core/services/order.service.ts`, `src/app/admin/admin-routing.module.ts`, `src/app/admin/admin.module.ts`, `src/app/admin/pages/orders/orders.component.ts|html|scss|spec.ts`, `src/app/admin/styles/_tables.scss`, `docs/ARCHITECTURE.md`
- **Decisión clave**: El detalle navega por `code` (no por `_id`) porque el endpoint `GET /:code` ya existía autenticado → cero cambios de API. Ruta de detalle (no modal): compartible, sobrevive al refresh (re-fetch por code) y le da sentido a la navegación atrás. La fila clicable usa `stopPropagation` en los botones de acción para no disparar la navegación; el código es un `<a>` real para accesibilidad/teclado. En specs, los componentes con `routerLink` necesitan `RouterTestingModule` (con `Router` provisto como spy, el `href` no se genera y las plantillas fallan con NG0303).
- **Verificación**: `ng test --watch=false` 91/91 OK (10 nuevos: 7 en order-detail, 3 en orders); `ng build --configuration production` OK sin warnings de budget.

### 2026-08-13 — feat — Admin: vistas separadas (Órdenes, Productos) y paginación server-side de órdenes
- **Descripción**: El dashboard ya no mezcla órdenes y productos en una sola vista. El admin queda con 4 páginas hijas del `AdminLayout`: `/admin` (métricas + últimas 5 pendientes con acciones rápidas y link a Órdenes), `/admin/orders` (lista con **filtro por estado** y **paginación**), `/admin/products` (lista read-only con categoría y stock) y `/admin/contact`. `GET /api/orders` ahora pagina server-side (`page`, `limit` default 10 máx 50, mantiene `?status=` y el barrido TTL) y responde `{ orders, total, page, limit, totalPages }`; `OrderService.getOrders(page?, limit?, status?)` devuelve `OrderPage`. El control de paginación incluye ‹ ›, números con elipsis para rangos grandes y "X–Y de Z". El dashboard recarga stats y pendientes tras confirmar/cancelar. Estilos de tabla compartidos extraídos a `admin/styles/_tables.scss`.
- **Archivos**: `backend/routes/orders.js`, `src/app/core/models/order.model.ts`, `src/app/core/services/order.service.ts`, `src/app/admin/admin-routing.module.ts`, `src/app/admin/admin.module.ts`, `src/app/admin/layout/admin-layout.component.html`, `src/app/admin/pages/orders/orders.component.ts|html|scss|spec.ts` (nuevo), `src/app/admin/pages/products/products.component.ts|html|scss|spec.ts` (nuevo), `src/app/admin/pages/dashboard/dashboard.component.ts|html|scss|spec.ts` (nuevo), `src/app/admin/styles/_tables.scss` (nuevo), `docs/ARCHITECTURE.md`
- **Decisión clave**: Paginación **server-side** (no client-side): las órdenes solo crecen y el `skip/limit` + `countDocuments` escala sin cargar todo el historial en el cliente; el frontend solo pide la página que muestra. Fallback: si una acción deja vacía la página actual, el componente retrocede a la última página válida. El admin quedó con cero specs → ahora 19 specs nuevos (10 órdenes, 4 productos, 5 dashboard).
- **Verificación**: `node --check` OK; paginación probada contra `lcapp-dev` (total/totalPages/orden desc/filtro pending correctos, datos de prueba limpiados); `ng test --watch=false` 81/81 OK; `ng build --configuration production` OK sin warnings de budget.

### 2026-08-13 — feat — Error de registro del pedido migrado a snackbar en cart-summary
- **Descripción**: El último aviso inline del flujo de contacto queda migrado al snackbar global. Cuando falla `createOrder`, `cart-summary` ahora muestra "No se pudo registrar tu pedido. Verifica tu conexión e inténtalo de nuevo." vía `snackbarService.show(..., 'error')` en vez del `<p class="status error">` que aparecía junto a los botones. Se eliminó el estado `errorMessage`, su reset en `openContact`, el render en el template y el estilo `.status.error` (sin uso).
- **Archivos**: `src/app/cart/components/cart-summary/cart-summary.component.ts|html|scss`, `cart-summary.component.spec.ts`, `docs/ARCHITECTURE.md`
- **Decisión clave**: Consistencia UX — todo el feedback de notificaciones del carrito pasa por el snackbar global (4s, `role=alert`); el error ya no compite con el estado "Registrando tu pedido…" dentro del resumen. Solo los estados "Verificando…"/"Registrando…" y los avisos bajo el código quedan inline (contenido posicional, no notificación).
- **Verificación**: `ng test` cart-summary 12/12 OK (test del error ahora espera el snackbar); `ng build --configuration production` OK sin warnings de budget.

### 2026-08-13 — feat — TTL perezoso: auto-cancelación de órdenes pendientes huérfanas
- **Descripción**: Las órdenes `pending` que quedan huérfanas (cliente que vacía el carrito o abandona el flujo) ya no viven para siempre: un barrido bajo demanda (`expireStalePendingOrders()`) pasa a `cancelled` toda `pending` con `createdAt` más antigua que `ORDER_TTL_HOURS` (default 48h). Se ejecuta al inicio de `GET /api/orders` (lista admin), `GET /api/orders/stats` y `GET /api/orders/:code/status` (público) — así, al ingresar al dashboard admin las huérfanas vencidas ya aparecen canceladas, y el cliente que verifica un código viejo ve `cancelled`. No se borran documentos (historial y stats conservados en `cancelled`); `PATCH /:code/items`, confirm/cancel admin y POST no cambian.
- **Archivos**: `backend/routes/orders.js`, `backend/.env.example`, `README.md`, `docs/ARCHITECTURE.md`
- **Decisión clave**: TTL **lazy** (evaluado al leer) en vez de cron de Render o TTL index de Atlas (que borra, no cancela). Funciona en el free tier de Render sin infra extra; tradeoff aceptado: solo corre cuando alguien pide datos (una pestaña de admin abierta sin recargar no lo dispara → mejora futura: cron).
- **Verificación**: `node --check` OK; sin cambios de frontend (no aplica `ng test`/`ng build`). Prueba manual pendiente contra `lcapp-dev` antes de cerrar la tarea.


### 2026-08-13 — feat — Snackbar con acción de deshacer y nuevos consumidores (vaciar carrito y agregar al carrito)
- **Descripción**: El snackbar ahora soporta un botón de acción opcional (`show(message, type, duration, actionLabel, onAction)`) que ejecuta el callback y cierra al pulsarlo (estilo Material). Nuevos consumidores: (1) "Vaciar carrito" en `cart-view` muestra "Tu carrito fue vaciado." con acción **Deshacer** que restaura el carrito completo — nuevo `CartService.restoreCart(snapshot)` que recupera items, `orderCode` y `orderModified` (snapshot tomado antes de vaciar, duración 5s para dar margen); (2) agregar al carrito desde catálogo (`product-detail` y `product-list`) muestra "«Nombre» agregado al carrito." conservando el feedback local existente (botón "✓ Agregado" y línea "¡Agregado al carrito!"). Además, los avisos "¡Pedido registrado!" (sin repetir el código, que ya está en el bloque superior) y "Modificaste tu carrito…" se reposicionaron justo debajo del bloque `cart-code` en `cart-summary`, fuera del área de contacto (Verificando/Registrando/error siguen junto a los botones).
- **Archivos**: `src/app/core/services/snackbar.service.ts|spec.ts`, `src/app/shared/components/snackbar/app-snackbar.component.ts|html|scss|spec.ts`, `src/app/core/services/cart.service.ts|spec.ts`, `src/app/cart/pages/cart-view/cart-view.component.ts` y `cart-view.component.spec.ts` (nuevo), `src/app/catalog/pages/product-detail/product-detail.component.ts` y `product-detail.component.spec.ts` (nuevo), `src/app/catalog/pages/product-list/product-list.component.ts` y `product-list.component.spec.ts` (nuevo), `src/app/cart/components/cart-summary/cart-summary.component.html`, `docs/ARCHITECTURE.md`
- **Decisión clave**: `actionLabel`/`onAction` opcionales en `SnackbarData` para no romper las llamadas existentes; el `show` sin acción no incluye las propiedades (se mantiene el objeto exacto que ya validaban los specs). Deshacer restaura el carrito completo (no solo items) para conservar la orden registrada tras un vaciado accidental. `restoreCart` reutiliza `emit` (spread + copia de items).
- **Verificación**: `ng build --configuration production` OK (sin warnings de budget), `ng test --watch=false` 62/62 specs OK (52 previas + 1 restoreCart + 1 show con acción + 1 acción en componente + 3 de cart-view + 4 de catálogo).

- **Descripción**: Nuevo componente de notificación tipo snackbar (Material): `AppSnackbarComponent` en `shared/components/snackbar` (exportado por `SharedModule`, renderizado una sola vez en `AppComponent`). Se dispara desde cualquier componente vía `SnackbarService` (`show(message, type, duration)` / `dismiss()`), que expone un `BehaviorSubject`. Tipos `success`/`error`/`info` (borde superior de acento con tokens `$color-success`/`$color-danger`/`$color-gold`), auto-cierre tras la duración (default 4s, timer reiniciable al emitir un nuevo `show`), botón ✕ con `aria-label`, `role=status` (o `role=alert` para errores) y `aria-hidden` cuando está vacío. **Responsive**: en móvil (< 600px) ancho completo anclado al borde inferior; en desktop se centra con `min-width: 344px` y `border-radius` (patrón Material). `OnPush` + `markForCheck`. Primer consumidor: `cart-view` reemplazó el mensaje inline del botón "Actualizar pedido" por el snackbar (éxito/error).
- **Archivos**: `src/app/shared/components/snackbar/app-snackbar.component.ts|html|scss|spec.ts` (nuevos), `src/app/core/services/snackbar.service.ts|spec.ts` (nuevos), `src/app/shared/shared.module.ts`, `src/app/app.component.html`, `src/app/cart/pages/cart-view/cart-view.component.ts|html|scss`
- **Decisión clave**: Snackbar controlado por **servicio global** (a diferencia del modal, que es presentacional con `@Input`/`@Output`) — el usuario lo eligió para poder disparar avisos desde cualquier componente sin estado local ni tree de bindings. El componente es singleton en `AppComponent`, fuera del `router-outlet`, para sobrevivir a la navegación. `show()` reemplaza el aviso actual y reinicia el timer (sin cola, suficiente para los flujos actuales).
- **Verificación**: `ng build --configuration production` OK (sin warnings de budget), `ng test --watch=false` 52/52 specs OK (43 previas + 5 de snackbar component + 4 de snackbar service).

### 2026-08-13 — feat — Actualización de la orden al editar el carrito (botón + auto-sync al contactar)
- **Descripción**: Cuando el admin le dice al cliente que no tiene tal producto, el cliente puede editar el carrito y propagar los cambios a la orden registrada. Se agregó endpoint público `PATCH /api/orders/:code/items` (solo órdenes `pending`; recalcula `total`; 404 si no existe, 400 si ya no está pendiente) y `OrderService.updateOrderItems()`. `Cart` rastrea `orderModified`: se setea al agregar/actualizar/quitar items si hay orden registrada, se resetea al registrar, al limpiar el código o con `markOrderSynced()`. Nuevo botón "Actualizar pedido" en `cart-view` (al lado de "Vaciar carrito"): visible solo si hay orden registrada, deshabilitado sin cambios pendientes, PATCHea los items actuales, muestra mensaje inline de éxito/error y deshabilita al sincronizar. Además, al contactar con la orden modificada (`openContact` en `cart-summary`) se hace auto-sync vía PATCH antes de redirigir; si el PATCH falla (400/404, la orden ya no es editable), se re-verifica el estado y el flujo existente (modal de éxito / aviso de cancelado-no encontrado) toma el control — sin duplicar órdenes ni lógica entre componentes. En el resumen se muestra el aviso "Modificaste tu carrito. Tu pedido se actualizará cuando vuelvas a contactar." cuando hay cambios pendientes. Se agregó el token `$color-success` (era un color hardcodeado en `cart-summary`).
- **Archivos**: `backend/routes/orders.js`, `src/app/core/services/order.service.ts`, `src/app/core/models/cart.model.ts`, `src/app/core/services/cart.service.ts`, `src/app/core/services/cart.service.spec.ts` (nuevo), `src/app/cart/pages/cart-view/cart-view.component.ts|html|scss`, `src/app/cart/components/cart-summary/cart-summary.component.ts|html|scss`, `src/app/cart/components/cart-summary/cart-summary.component.spec.ts`, `src/styles/_variables.scss`, `docs/ARCHITECTURE.md`
- **Decisión clave**: Sincronización lazy (al contactar o con el botón explícito), no en cada clic de `+`/`−` — consistente con el patrón existente (la orden se materializa en el contacto). El botón solo sincroniza items, nunca invalida el `orderCode`; los casos confirmado/cancelado/404 quedan a cargo del flujo de verificación existente, evitando duplicar lógica entre `cart-view` y `cart-summary`.
- **Verificación**: `ng build --configuration production` OK (budget SCSS respetado), `ng test --watch=false` 43/43 specs OK (27 previas + 13 de cart.service + 3 de auto-sync en cart-summary).

### 2026-08-13 — feat — Avisos de pedido cancelado/no encontrado como modal y reposicionamiento de "Vaciar carrito"
- **Descripción**: El aviso de orden cancelada o no encontrada dejó de ser un `<p>` dentro del resumen y ahora usa el modal compartido `AppModalComponent` (con botón "Entendido", close ✕, backdrop y Escape activos; título dinámico: "Pedido cancelado" / "Pedido no encontrado"). El botón "Vaciar carrito" se movió del pie de página al final de la lista de productos (dentro de la columna `.items`, con `align-self: flex-start`), quedando arriba del resumen en móvil y al pie de los productos en desktop.
- **Archivos**: `src/app/cart/components/cart-summary/cart-summary.component.ts|html|scss`, `src/app/cart/components/cart-summary/cart-summary.component.spec.ts`, `src/app/cart/pages/cart-view/cart-view.component.html|scss`
- **Decisión clave**: Los avisos reutilizan el modal compartido ya existente (consistencia con el de redirección y el de éxito). `resetRegistration(notice, title)` ahora recibe el título; `closeNotice()` limpia ambos estados. En los specs, el caso cancelado usa `Subject` (no `of`) para emitir el estado tras el primer cambio de detección, igual que en producción (HTTP async) — con `of` síncrono los tres modales disparan `ngOnChanges` en el mismo pass y el `modal-lock` queda removido.
- **Verificación**: `ng build --configuration production` OK (budget SCSS respetado tras compartir estilos entre `.success-notice` y `.notice`), `ng test --watch=false` 27/27 specs OK.

### 2026-08-13 — feat — Verificación del estado real de la orden al volver al carrito
- **Descripción**: El carrito mostrado al volver siempre decía "¡Pedido registrado!" con el mismo código, aunque el admin hubiera confirmado o cancelado la orden. Se agregó endpoint público `GET /api/orders/:code/status` en el backend que devuelve solo `{ code, status, confirmedAt }` (sin datos de cliente ni items). Al montar el carrito con orden registrada, `CartSummaryComponent` consulta ese estado: si está `confirmed` muestra un modal de éxito ("Su compra fue todo un éxito. Muchas gracias.") con botón "Volver al inicio", vacía el carrito completo y redirige a home (auto tras ~4s); si está `cancelled` o la orden no existe (404), limpia solo el `orderCode` del carrito (conserva los items) y muestra un aviso — los botones de contacto quedan activos para registrar una orden nueva. Un error de red (no 404) mantiene el estado actual sin romper la UI. `OrderService.getOrderStatus()` consulta el nuevo endpoint y `CartService.clearOrderCode()` limpia solo el código. `handleError` de `OrderService` ahora preserva el `status` HTTP en el error para distinguir 404 de fallas de red.
- **Archivos**: `backend/routes/orders.js`, `src/app/core/services/order.service.ts`, `src/app/core/services/cart.service.ts`, `src/app/core/models/order.model.ts`, `src/app/cart/components/cart-summary/cart-summary.component.ts|html|scss`, `src/app/cart/components/cart-summary/cart-summary.component.spec.ts`
- **Decisión clave**: El endpoint de estado es público (solo expone `code`/`status`/`confirmedAt`, sin PII) para que el cliente verifique sin autenticación. La ruta `/:code/status` se registra antes que la admin `/:code` para no colisionar. Vaciar el carrito completo vs. solo el código según el estado: `confirmed` → `clearCart()` + redirect home; `cancelled`/404 → `clearOrderCode()` para que el cliente pueda volver a contactar.
- **Verificación**: `ng build --configuration production` OK, `ng test --watch=false` 27/27 specs OK (22 previas + 5 nuevas de estados de orden).

### 2026-08-13 — feat — Preaviso de redirección a canales de contacto con modal compartido
- **Descripción**: Al pulsar WhatsApp/Instagram/Telegram en el resumen del carrito, en lugar de abrir el canal inmediatamente se muestra un modal de aviso "Serás redirigido a X…" con spinner. El modal se abre al tocar el canal, espera a que el endpoint `POST /api/orders` responda (si el pedido no estaba registrado), luego espera 2 segundos y recién entonces abre el canal en pestaña nueva. Si el endpoint falla, se cierra el modal y se muestra el error sin redirigir. Se creó `AppModalComponent` como componente compartido en `shared/components/modal` (overlay con backdrop, cierre por botón ✕ / backdrop / Escape configurable, foco en el panel, bloqueo de scroll del body con `body.modal-lock`, a11y `role=dialog`). La redirección final usa un `<a target="_blank">` creado y clickeado programáticamente en vez de `window.open` (más robusto contra popup blockers con espera asíncrona). El timer de 2s se cancela con `takeUntil` en `ngOnDestroy` si el usuario navega.
- **Archivos**: `src/app/shared/components/modal/app-modal.component.ts|html|scss` (nuevos), `src/app/shared/components/modal/app-modal.component.spec.ts` (nuevo), `src/app/shared/shared.module.ts`, `src/styles/_base.scss` (`body.modal-lock`), `src/app/cart/components/cart-summary/cart-summary.component.ts|html|scss`, `src/app/cart/components/cart-summary/cart-summary.component.spec.ts` (nuevo)
- **Decisión clave**: El modal es un componente presentacional controlado por el padre (`@Input open` + `@Output close`), sin lógica de negocio — el cart-summary orquesta el flujo (endpoint → delay → redirect). Sin librerías de overlay (CDK/ng-bootstrap): CSS puro con tokens existentes.
- **Verificación**: `ng build --configuration production` OK, `ng test --watch=false` 22/22 specs OK (8 previas + 14 nuevas: 10 de modal + 4 de cart-summary).

### 2026-08-13 — style — Layout admin con sidebar lateral y vistas responsive
- **Descripción**: El admin dejó de ser dos páginas sueltas con un nav improvisado al pie del dashboard. Se creó `AdminLayoutComponent` (`admin/layout/`) que envuelve las rutas hijas con un sidebar fijo (Dashboard, Configuración de contacto, Cerrar sesión); en móvil (< 768px) el sidebar colapsa a un drawer con botón hamburguesa + backdrop. El routing de `admin` ahora usa un componente padre con `children`. El dashboard perdió su nav inferior y el logout (viven en el layout) y es responsive: métricas con `flex-wrap` y tablas con wrapper `.table-scroll` (scroll horizontal en pantallas angostas). `contact-settings` dejó de depender de la clase global `container`. En móvil, el botón hamburguesa se movió **dentro** del sidebar (anclado a su borde derecho, `right: -2.75rem`) para que viaje con el nav como un manubrio: cerrado sobresale del borde izquierdo de la pantalla y abierto flota junto al borde del drawer sin tapar la marca "LC Admin". El icono cambia entre hamburguesa y ✕ según el estado (`sidebarOpen`) y el `aria-label` también. `AppComponent` detecta la ruta `/admin` (`router.events` + `NavigationEnd`) y oculta el header público (con el carrito) y el footer en la vista admin.
- **Archivos**: `src/app/admin/layout/admin-layout.component.ts|html|scss` (nuevo), `src/app/admin/admin-routing.module.ts`, `src/app/admin/admin.module.ts`, `src/app/admin/pages/dashboard/dashboard.component.html|scss|ts`, `src/app/admin/pages/contact-settings/contact-settings.component.html`, `src/app/app.component.ts|html`, `docs/ARCHITECTURE.md`
- **Decisión clave**: Patrón layout + children con `router-outlet` anidado (mismo patrón que el resto de módulos lazy). Sidebar fijo en desktop, drawer en móvil con un único breakpoint (768px) — sin librerías, solo CSS + `*ngIf` para el backdrop.
- **Verificación**: `ng build --configuration production` OK, 8/8 specs OK.

### 2026-08-13 — feat — Contacto configurable desde el admin (API, no mock)
- **Descripción**: El contacto del negocio (WhatsApp/Instagram/Telegram) dejó de ser constante de build + localStorage y pasa a ser dato persistido en MongoDB (`Config`, doc único `site`) con API `GET /api/config` (público) y `PUT /api/config` (admin JWT). En frontend, `MockContactService` se reemplazó por `HttpContactService` (misma abstracción `ContactService`, cachea en `BehaviorSubject` con un solo GET por sesión). Nueva página admin `contact-settings` en `/admin/contact` (FormModule) con link desde el dashboard. Se eliminó `contact` de `environment.ts`/`environment.prod.ts` y del `deploy.yml` (secrets `WHATSAPP_*`/`INSTAGRAM_*`/`TELEGRAM_*` obsoletos). `ContactConfig` se movió a `core/models/contact.model.ts`. Seed siembra la config con placeholders.
- **Archivos**: `backend/models/Config.js` (nuevo), `backend/routes/config.js` (nuevo), `backend/server.js`, `backend/seed.js`, `src/app/core/services/http-contact.service.ts` (nuevo), `src/app/core/services/http-contact.service.spec.ts` (nuevo), `src/app/core/services/mock-contact.service.ts` (eliminado), `src/app/core/services/contact.service.ts`, `src/app/core/models/contact.model.ts` (nuevo), `src/app/core/models/environment.model.ts`, `src/app/app.module.ts`, `src/app/app.component.ts`, `src/app/app.component.spec.ts`, `src/app/cart/components/cart-summary/cart-summary.component.ts`, `src/app/admin/pages/contact-settings/*` (nuevo), `src/app/admin/admin.module.ts`, `src/app/admin/admin-routing.module.ts`, `src/app/admin/pages/dashboard/*`, `src/environments/environment.ts`, `src/environments/environment.prod.ts`, `.github/workflows/deploy.yml`, `angular.json` (fix preexistente: faltaba `stylePreprocessorOptions.includePaths` en target test), `docs/adr/006-contacto-configurable-admin.md` (nuevo), `README.md`, `docs/ARCHITECTURE.md`
- **Decisión clave**: La abstracción `ContactService` se diseñó para esto — el reemplazo del mock por la implementación HTTP fue un cambio de provider en `AppModule`. El contacto es dato de negocio (base), no config de build (env). Pruebas: build producción OK, 8/8 specs OK, flujo local verificado (login, PUT 401 sin token / 200 con token, GET público, orden CAR-XXXXX confirmada con descuento de stock 25→23) contra la base `lcapp-dev`.

### 2026-08-12 — config — Dev local aislado de producción con DB `lcapp-dev` en Atlas
- **Descripción**: Para probar localmente sin ensuciar los datos reales, el `backend/.env` local ahora apunta su `MONGODB_URI` a la base `lcapp-dev` (mismo cluster M0 de Atlas, base separada de la de producción `lcapp`). Se corrió `yarn seed` una vez (4 categorías, 9 productos) y se verificó que el backend local sirve `GET /api/health`, `/api/categories` y `/api/products` desde esa base. Producción (Render + Atlas `lcapp`) no se toca porque lee sus propias env vars.
- **Archivos**: `backend/.env` (solo local, gitignoreado), `README.md`, `docs/ARCHITECTURE.md`
- **Decisión clave**: Aislamiento de dev con costo cero: en lugar de instalar MongoDB local o Docker (no disponible en la máquina) o un json-server (que no puede replicar la lógica de negocio del backend: JWT, descuento de stock, stats), se usa una segunda base en el mismo cluster gratuito de Atlas. json-server quedó descartado porque el frontend depende de lógica de negocio del backend, no de CRUD plano.

### 2026-08-12 — config — Skill docs-sync + regla de documentación en AGENTS.md
- **Descripción**: Para que la documentación no vuelva a desactualizarse, se creó el skill `skills/docs-sync/SKILL.md` (mismo formato que `no-env-leak`) con una tabla de decisión que mapea cada tipo de cambio con el doc que debe actualizarse, y una regla obligatoria en `AGENTS.md` que obliga a sincronizar `README.md`/`docs/ARCHITECTURE.md` antes de dar por terminada la tarea. Se agregó el check de docs al pre-flight checklist.
- **Archivos**: `skills/docs-sync/SKILL.md` (nuevo), `AGENTS.md`
- **Decisión clave**: La doc se mantiene con dos niveles (README = quickstart, ARCHITECTURE = mapa) y se actualiza solo cuando el cambio toca estructura — no en cada micro-fix. La regla vive en AGENTS.md y el skill la ejecuta, igual que el patrón `change-history`.

### 2026-08-12 — docs — Mapa del mundo: README actualizado y ARCHITECTURE.md
- **Descripción**: El README quedó anclado en la etapa "frontend-only con JSONs" y desorientaba al entrar al repo. Se actualizó al estado real (stack Angular + Node/Express + MongoDB, estructura con `backend/`, cómo correr los dos procesos, despliegue Pages/Render/Atlas, tabla de endpoints y secrets por entorno). Se creó `docs/ARCHITECTURE.md` como mapa del sistema completo: diagrama de piezas, flujo de compra end-to-end (carrito → código CAR-XXXXX → contacto → orden → confirmación con descuento de stock), frontera de datos (core/services), decisión de sesión en memoria + storage best-effort, y variables de entorno sin valores reales.
- **Archivos**: `README.md`, `docs/ARCHITECTURE.md` (nuevo)
- **Decisión clave**: La documentación de onboarding va en dos niveles — README = puerta de entrada (quickstart), ARCHITECTURE.md = mapa profundo (cómo funciona el todo). Sin secretos en los docs; solo nombres de variables.

### 2026-08-12 — fix — Login falla cuando localStorage está lleno (QuotaExceededError)
- **Descripción**: El login contra el backend respondía 200 pero el frontend mostraba "No se pudo iniciar sesión. Intenta de nuevo." Causa raíz: el `tap()` que persistía el token en `localStorage` lanzaba `QuotaExceededError` (storage lleno), la excepción caía en `catchError` y se mostraba el mensaje genérico aunque el POST hubiera sido exitoso. Se hizo `AuthService` resiliente: la sesión vive en memoria (`BehaviorSubject`) y `localStorage` es persistencia best-effort dentro de `try/catch`; `isAuthenticated()`/`getToken()` usan memoria primero y storage como fallback.
- **Archivos**: `src/app/core/services/auth.service.ts`
- **Decisión clave**: El estado de sesión nunca debe depender de que `localStorage` funcione; si la escritura falla la sesión sigue activa en memoria (se pierde al recargar) en vez de bloquear el login.

### 2026-08-12 — chore — Script npm run hash para cambiar la clave admin
- **Descripción**: Script `npm run hash -- "clave"` que genera un hash bcrypt para `ADMIN_PASSWORD_HASH`, validando longitud mínima de 8 caracteres. Facilita el cambio futuro de la clave del admin sin depender de generar el hash a mano.
- **Archivos**: `backend/hash-password.js` (nuevo), `backend/package.json`
- **Decisión clave**: La clave del admin no vive en el código sino en env (`ADMIN_PASSWORD_HASH`); cada ejecución de bcrypt produce un hash distinto y válido del mismo password.

### 2026-08-12 — feat — Admin: login JWT y gestión de órdenes
- **Descripción**: El panel admin ahora se protege con JWT real (`POST /api/auth/login`). Se añadió middleware `authenticate` en las rutas admin de órdenes (GET /, GET /stats, GET /:code, PATCH confirm/cancel); el POST de creación de orden sigue público para el carrito. El frontend deja el mock: `AuthService` consume la API, un interceptor inyecta `Authorization: Bearer` y el dashboard muestra métricas de órdenes, listado con estado, y acciones confirmar/cancelar.
- **Archivos**: `backend/routes/auth.js` (nuevo), `backend/middleware/auth.js` (nuevo), `backend/routes/orders.js`, `backend/server.js`, `backend/.env.example`, `backend/package.json`; `src/app/core/interceptors/auth.interceptor.ts` (nuevo), `src/app/core/services/auth.service.ts`, `src/app/core/services/order.service.ts`, `src/app/core/models/order.model.ts`, `src/app/core/models/user.model.ts`, `src/app/app.module.ts`, `src/app/auth/pages/login/*`, `src/app/admin/pages/dashboard/*`
- **Decisión clave**: Credenciales de admin vía env (`ADMIN_USER` + `ADMIN_PASSWORD_HASH` bcrypt + `JWT_SECRET`), sin modelo de usuarios en Mongo. Confirmar orden descuenta stock; cancelar solo aplica a pendientes.

### 2026-08-12 — feat — Carrito registra pedido en el backend al contactar
- **Descripción**: Al tocar WhatsApp/Instagram/Telegram en el resumen del carrito, el pedido se registra en el backend (`POST /api/orders`) con un estado loading; al confirmarse, se guarda el código real del backend en el carrito y se abre el canal con ese código. Anti-duplicados: si el carrito ya registró una orden, reutiliza el código sin re-enviar. `customerName` pasó a ser opcional (default `Cliente web`) porque el registro es anónimo hasta la conversación.
- **Archivos**: `backend/routes/orders.js`, `src/app/core/models/order.model.ts`, `src/app/core/models/cart.model.ts`, `src/app/core/services/order.service.ts`, `src/app/core/services/cart.service.ts`, `src/app/cart/components/cart-summary/*`
- **Decisión clave**: El registro es anónimo (no se pide nombre/teléfono); la confirmación y los datos del cliente se resuelven por la conversación, y el admin aplica el estado (aprobado/rechazado) desde su panel.

### 2026-08-12 — feat — Catálogo consume la API del backend
- **Descripción**: `CatalogService` dejó de leer `assets/data/*.json` y ahora consume la API (`environment.apiUrl` + `/categories`, `/products`). Se agregó `apiUrl` al modelo `Environment` y al CI (inyectado por secret `API_URL` en prod; local apunta a `localhost:3000` para desarrollo con el backend o Mockoon). El backend ajustó su shape para cumplir el contrato del frontend: `categoryId` como string (se quitó el populate) y `id` generado por imagen en el `toJSON`. CORS del backend ahora configurable por env var `CORS_ORIGIN` (por defecto `*`).
- **Archivos**: `src/app/core/services/catalog.service.ts`, `src/app/core/models/environment.model.ts`, `.github/workflows/deploy.yml`, `backend/models/Product.js`, `backend/routes/products.js`, `backend/server.js`, `backend/.env.example`
- **Decisión clave**: Dev y prod se separan vía `environment`: local usa `http://localhost:3000/api` (backend real o Mockoon en el mismo puerto); prod usa la URL de Render inyectada por el CI. El contrato de datos lo define el frontend (shape de los JSON que ya existían).

### 2026-08-08 — config — Base de datos MongoDB: `lcapp` en vez de `test`
- **Descripción**: La connection string en `backend/.env` no especificaba nombre de base → MongoDB guardaba los datos en `test` (default). Se agregó `/lcapp` antes del `?` para usar una base con nombre propio y se re-ejecutó el seed (4 categorías, 9 productos). La base `test` (con la orden de prueba `CAR-BHOE7`) se elimina desde Atlas Data Explorer.
- **Archivos**: `backend/.env` (gitignoreado, no se commitea)
- **Decisión clave**: Nunca dejar que Mongo use la base default `test` — el nombre de la base va en la connection string. El seed solo toca catálogo; las órdenes sobreviven a un re-seed.

### 2026-08-08 — feat — Backend Node.js + Express + MongoDB
- **Descripción**: Se creó el backend en `backend/` (Express + Mongoose): modelos Category/Product/Order (basados en los JSON de assets y el ADR-009), rutas de catálogo (GET categories/products), órdenes completas (crear con código `CAR-XXXXX`, confirmar con descuento de stock, cancelar, stats) y script de seed que carga los datos de assets a MongoDB. Conectado a MongoDB Atlas (cluster M0 FREE) y verificado end-to-end.
- **Archivos**: `backend/` completo (`server.js`, `seed.js`, `models/`, `routes/`, `package.json`, `package-lock.json`, `.env.example`)
- **Decisión clave**: Los modelos devuelven `id` (no `_id` de Mongo) vía `toJSON.transform` para mantener el contrato del frontend. `backend/.env` está en `.gitignore` (no-env-leak). `.gitignore` ahora ignora `node_modules/` en cualquier nivel (antes solo `/node_modules` raíz).

### 2026-08-08 — docs — ADR-005: backend Node.js + Express + MongoDB
- **Descripción**: Se heredó del proyecto anterior (catalogApp) el ADR-005 que decide Node.js + Express + MongoDB como backend futuro. El backend vivirá en `LCApp/backend/` dentro del mismo repo. Se adaptó al formato local de ADR y al contexto actual de LCApp.
- **Archivos**: `docs/adr/005-backend-nodejs-mongodb.md`
- **Decisión clave**: No se reabre el debate de stack — se hereda la decisión ya tomada (Node+Express+Mongo) y se registra el costo de migración del auth mock a JWT real.

### 2026-08-08 — fix — CI: servir SPA en GitHub Pages con 404.html
- **Descripción**: Navegar directamente a `/LCApp/admin` (o cualquier subruta) daba 404 en GitHub Pages porque el servidor estático busca un archivo físico `admin/index.html` que no existe. Se agregó un paso al workflow que copia `index.html` → `404.html`. GitHub Pages sirve `404.html` ante cualquier ruta inexistente → Angular arranca y resuelve la ruta del SPA. URLs limpias, sin hash routing.
- **Archivos**: `.github/workflows/deploy.yml`
- **Decisión clave**: Opción A (404.html trick) sobre HashLocationStrategy para no cambiar el código de la app ni ensuciar las URLs.

### 2026-08-08 — docs — Flujo de trabajo con Git (rama + PR)
- **Descripción**: Se documentó en el README la política de ramas y PR ahora que `master` está protegida (push directo bloqueado, requiere PR con aprobación). Incluye naming de ramas (`type/descripcion`), conventional commits y pasos del flujo.
- **Archivos**: `README.md`
- **Decisión clave**: La protección de rama es config de GitHub (no se versiona); el README la documenta para que el flujo sea reproducible por cualquier colaborador.

### 2026-08-08 — fix — CI: inyectar también environment.ts de desarrollo
- **Descripción**: El workflow fallaba con `MissingFileReplacementException: src/environments/environment.ts path in file replacements does not exist`. El `fileReplacements` de `angular.json` valida que existan tanto la entrada (`environment.ts`) como la salida (`environment.prod.ts`), y ninguna existe en el checkout (ambas gitignoreadas, git no rastrea directorios vacíos). Se amplió el paso de inyección para crear ambos archivos: `environment.ts` con placeholders vacíos (nunca usado en build prod) y `environment.prod.ts` con los secrets.
- **Archivos**: `.github/workflows/deploy.yml`
- **Decisión clave**: Mantiene `no-env-leak` (ningún environment se commitea; solo existen en el checkout del CI). El `environment.ts` con valores vacíos es seguro porque el build de producción usa `fileReplacements` y solo se compila el `.prod.ts`.

### 2026-08-08 — fix — CI: crear src/environments antes de inyectar secrets
- **Descripción**: El workflow de GitHub Pages fallaba con `ENOENT: src/environments/environment.prod.ts`. Causa raíz: el directorio no existe en el checkout del CI (ambos environments están gitignoreados y git no rastrea carpetas vacías). Se añadió `fs.mkdirSync('src/environments', { recursive: true })` antes del `writeFileSync`.
- **Archivos**: `.github/workflows/deploy.yml`
- **Decisión clave**: `mkdirSync` con `recursive: true` es idempotente, así que cubre tanto el checkout del CI (directorio ausente) como builds locales (directorio presente).

### 2026-08-08 — docs — README real + CI de GitHub Pages
- **Descripción**: `README.md` pasó del boilerplate de Angular CLI a la documentación real del proyecto (stack, funcionalidades, cómo correr, configuración de contacto, estructura y convenciones). Se creó `.github/workflows/deploy.yml` para deploy automático a GitHub Pages en push a `master`: instala deps, inyecta `environment.prod.ts` desde GitHub Secrets (`WHATSAPP_NUMBER`, `WHATSAPP_DISPLAY`, `INSTAGRAM_HANDLE`, `TELEGRAM_HANDLE`) y publica `dist/catalog`.
- **Archivos**: `README.md`, `.github/workflows/deploy.yml`
- **Decisión clave**: El workflow replica el patrón probado del proyecto viejo (catalogApp) y respeta `no-env-leak` (los datos reales solo entran por secrets en CI; el `environment.prod.ts` local permanece gitignoreado). La salida es `dist/catalog` (Angular 15), no `dist/catalog/browser` como en Angular 19.

### 2026-08-08 — feat — Header cliente: solo marca + carrito con badge
- **Descripción**: El header dejó de mostrar Inicio, Carrito, Admin, Ingresar y el estado de sesión: el cliente no debe saber que existen el panel admin ni el login. Ahora solo muestra la marca (lleva a la home) y un icono de carrito (SVG inline, sin librería de iconos) con badge con la cantidad de productos, navegando a `/cart`. Se eliminó el uso de `AuthService` en el componente; las rutas `/admin` y `/login` siguen accesibles por URL para el dueño.
- **Archivos**: `src/app/shared/components/header/header.component.ts`, `header.component.html`, `header.component.scss`
- **Decisión clave**: La navegación de gestión (admin/login) queda fuera del alcance del cliente; el header público se reduce a marca + carrito. El SVG es inline para no depender de una librería de iconos.

### 2026-08-08 — docs — Enmienda ADR-002 tras migración a assets JSON
- **Descripción**: En lugar de crear un ADR nuevo, se enmendó `docs/adr/002-frontera-datos-observables.md` para reflejar que la frontera de catálogo ya usa `HttpClient` sobre assets JSON (la decisión "hoy mock, mañana HttpClient" se cumplió). Cambios menores de implementación (breadcrumbs, enlaces de redes, budgets) no requieren ADR propio: el razonamiento queda en `HISTORIAL.md`.
- **Archivos**: `docs/adr/002-frontera-datos-observables.md`
- **Decisión clave**: Los ADR registran decisiones de arquitectura con sus alternativas; el historial registra toda tarea. Solo se crea ADR nuevo ante una decisión de arquitectura real.

### 2026-08-08 — refactor — Datos de catálogo movidos a assets JSON + HttpClient
- **Descripción**: Categorías y productos dejaron de estar hardcodeados en `CatalogService` y pasaron a `src/assets/data/categories.json` y `products.json`. El servicio ahora usa `HttpClient` (rutas relativas) con manejo de error unificado (`handleError` que loguea y devuelve un `Error` claro). Se registró `HttpClientModule` en `AppModule`. Los consumidores (`category-list`, `product-list`, `product-detail`, `dashboard`) muestran ahora estados visibles de carga y error en lugar de tragar fallos.
- **Archivos**: `src/assets/data/categories.json`, `src/assets/data/products.json`, `src/app/core/services/catalog.service.ts`, `src/app/app.module.ts`, `src/app/home/pages/category-list/`, `src/app/catalog/pages/product-list/`, `src/app/catalog/pages/product-detail/`, `src/app/admin/pages/dashboard/`
- **Decisión clave**: Datos ≠ código: el contenido del catálogo vive en assets y el servicio es la única frontera (`HttpClient` hoy, API remota mañana sin tocar componentes). El contacto NO migra a assets porque es configuración (vive en `environment` + `localStorage`).

### 2026-08-08 — feat — Breadcrumb completo en detalle: Inicio > Categoría > Producto
- **Descripción**: El breadcrumb del detalle de producto solo mostraba Inicio > Producto. Ahora carga la categoría desde `categoryId` de la ruta y muestra Inicio > Nombre de categoría (enlazado al listado `/catalog/:categoryId`) > Nombre de producto. Se centralizó la construcción en `updateBreadcrumb()` que se recalcula cuando llega la categoría o el producto.
- **Archivos**: `src/app/catalog/pages/product-detail/product-detail.component.ts`
- **Decisión clave**: El breadcrumb de detalle usa los tres niveles disponibles en la ruta anidada, con la categoría como link intermedio.

### 2026-08-08 — feat — Breadcrumb en el listado de productos por categoría
- **Descripción**: El breadcrumb solo aparecía en el detalle de producto. Se agregó a `product-list` (Inicio > Nombre de categoría) cargando el nombre desde `getCategoryById` al inicializar y reemplazando el enlace "← Volver a categorías". Se mantiene el mismo componente `app-breadcrumbs` de `SharedModule`.
- **Archivos**: `src/app/catalog/pages/product-list/product-list.component.ts`, `src/app/catalog/pages/product-list/product-list.component.html`
- **Decisión clave**: El listado muestra el breadcrumb con la categoría como item actual (sin link), idéntico al patrón del detalle.

### 2026-08-08 — fix — Enlaces de redes activos con datos por defecto + merge sin vacíos
- **Descripción**: El botón WhatsApp del carrito no navegaba porque `MockContactService` devolvía contactos vacíos: el environment tenía `''` (los links caían a `#`). Se llenaron `environment.ts` y `environment.prod.ts` con datos de ejemplo (mismo placeholder del proyecto viejo: `+52 123 456 7890`, `@tu_usuario`) y se corrigió el merge en `getStoredContact()` para que un `contact_config` previo en `localStorage` con strings vacíos no pise los defaults del environment.
- **Archivos**: `src/environments/environment.ts`, `src/environments/environment.prod.ts`, `src/app/core/services/mock-contact.service.ts`
- **Decisión clave**: Los valores de ejemplo permiten que wa.me/instagram/telegram abran de verdad; el negocio real los reemplaza localmente o por CI vía GitHub Secrets (regla `no-env-leak`).

### 2026-08-08 — config — Environment de contacto + seeds para enlaces de redes sociales
- **Descripción**: Se creó `src/environments/` con `environment.ts` (dev) y `environment.prod.ts` (prod) para configurar el contacto del negocio (número WhatsApp, Instagram, Telegram). Se conectó `fileReplacements` en `angular.json` (config producción) y se amplió `Environment` en `core/models/environment.model.ts`. `MockContactService` ahora siembra los defaults desde `environment.contact` (con override por `localStorage`), de modo que los enlaces de WhatsApp/Instagram/Telegram del footer y del resumen del carrito dejan de caer en `#` y navegan de verdad: WhatsApp abre `wa.me/<número>` con mensaje prefabricado que incluye el código de carrito, Instagram/Telegram abren el perfil.
- **Archivos**: `src/environments/environment.ts`, `src/environments/environment.prod.ts`, `src/app/core/models/environment.model.ts`, `src/app/core/services/mock-contact.service.ts`, `angular.json`, `.gitignore`
- **Decisión clave**: Siguiendo `no-env-leak`, ambos environment quedan en `.gitignore` con placeholders vacíos; los valores reales del negocio se cargan localmente (nunca commiteados) y/o se inyectan por CI en build.

### 2026-08-08 — feat — Flujo de compra por código + detalle de producto y contacto configurables
- **Descripción**: El carrito deja de ser un mapa y pasa a ser una entidad persistida con código (`CAR-XXXXX`) con la que el cliente finaliza la compra por redes sociales; se agregó el detalle de producto (galería, descuentos, stock) y la config de contacto (WhatsApp/Instagram/Telegram) consumida por el footer y el resumen del pedido. Se migró `CatalogService` a Observables y se añadió `getProductById`, `getCategoryById`, `getProducts`; los consumidores (`category-list`, `product-list`, `dashboard`) migraron de `.then()` a `subscribe`. Nueva frontera de datos de contacto: abstracción `ContactService` + mock persistido en `localStorage` inyectado en `AppModule`. Se crearon `cart-item` y `cart-summary` (resumen con código, envío gratis, subtotal con descuento, botones de redes con mensaje prefabricado) y la vista `cart-view` con breadcrumbs, estados vacío/carga y feedback visual. Pipe `currencyFormat` (MXN) y `Breadcrumbs` movidos a `SharedModule`. Ruta de detalle anidada `catalog/:categoryId/product/:productId` con breadcrumb. Footer de `AppComponent` con datos de contacto.
- **Archivos**: `src/app/core/models/` (cart, environment, product, category, breadcrumb), `src/app/core/services/` (cart, catalog, contact, mock-contact), `src/app/catalog/` (módulo, rutas, product-list, product-detail, product-gallery), `src/app/cart/` (módulo, cart-view, cart-item, cart-summary), `src/app/shared/` (pipes/currency-format, components/breadcrumbs, shared.module), `src/app/home/pages/category-list/`, `src/app/admin/pages/dashboard/`, `src/app/app.module.ts`, `src/app/app.component.{ts,html,scss}`, `src/styles/_variables.scss` (`$color-footer-text-muted`), `angular.json` (budget `anyComponentStyle` 3kb/6kb), `docs/adr/001-flujo-compra-codigo.md`, `docs/adr/002-frontera-datos-observables.md`
- **Decisión clave**: Compra sin pasarela: el código de carrito es la pieza de trazabilidad del pedido (ADR-001); frontera de datos reactiva uniforme (Observables) y reemplazable por `HttpClient` cambiando una línea en `AppModule` (ADR-002).

### 2026-08-08 — style — Fix layout: hero full-bleed y contenido centrado en contenedor
- **Descripción**: Se corrigió el margen enorme que se veía en pantallas anchas: el `<main>` dejó de imponer el `.container` de 1100px sobre todas las rutas, así el hero de la home ahora es full-bleed (gradiente de borde a borde). Cada página aplica `.container` a su contenido interno; el `.container` global solo da padding horizontal.
- **Archivos**: `src/app/app.component.html`, `src/styles/_base.scss`, `src/app/home/pages/category-list/` (`html` + `scss`), `src/app/catalog/pages/product-list/`, `src/app/cart/pages/cart-view/`, `src/app/auth/pages/login/`, `src/app/admin/pages/dashboard/` (html)
- **Decisión clave**: El ancho de contenido ya no se impone desde el shell (`app-root`); el componente de página decide si usa `.container` (centrado 1100px) o full-bleed, replicando el patrón del proyecto anterior.

### 2026-08-08 — style — Rebranding visual: paleta "L'Essence de Cerise"
- **Descripción**: Se trajo el design system del proyecto anterior (catalogApp) y se aplicó al cascarón vanilla. Paleta cerise/gold sobre fondo crema (`#8B2252`, `#C9A96E`, `#D4739D`, `#F5E6E0`), tipografías Google Fonts (DM Serif Display para títulos + DM Sans para texto), hero con gradiente rosa, header crema translúcido con monograma "LC", cards blancas con borde dorado y hover elevado, badges gold pill, precios dorados y footer cherry. Los componentes renombrados a `.scss` migran a la paleta nueva; se eliminaron tokens obsoletos (`$color-success`, etc.).
- **Archivos**: `src/styles/_variables.scss`, `src/styles/_base.scss`, `src/index.html`, `shared/components/header/`, `home/pages/category-list/`, `catalog/pages/product-list/`, `cart/pages/cart-view/`, `auth/pages/login/`, `admin/pages/dashboard/`, `app.component.{html,scss,ts}`
- **Decisión clave**: La marca se expresa exclusivamente vía tokens en `_variables.scss`; ningún color/letra hardcodeado en los componentes. El layout de `app-root` pasa a flex column con `main { flex: 1 }` para anclar el footer al fondo.

### 2026-08-08 — docs — Import de skills y reglas del proyecto anterior (catalogApp)
- **Descripción**: Se rescataron y adaptaron al stack vanilla las convenciones del proyecto viejo (Angular 19 + Tailwind, descartado). Se crearon los skills de proyecto `catalog-component` (workflow modelo → mock → servicio → componente → ruta → build, con NgModules + SCSS) y `no-env-leak` (nunca commitear `environment*.ts` con datos reales). Se extendió `AGENTS.md` con commit policy, política de entornos/secretos, pre-flight checklist, manejo de errores, accesibilidad, performance, testing y convención ADR. El viejo `changelog-discipline` ya estaba cubierto por `change-history`.
- **Archivos**: `skills/catalog-component/SKILL.md`, `skills/no-env-leak/SKILL.md`, `AGENTS.md`, `.atl/skill-registry.md`
- **Decisión clave**: Los skills nuevos viven en `skills/` del proyecto (scope `project`) y quedan indexados en el registro; el ADR de cada decisión de arquitectura futura se registrará en `docs/adr/`.

### 2026-08-08 — refactor — Migración de CSS puro a SCSS con tokens compartidos
- **Descripción**: El proyecto deja de usar CSS puro y pasa a SCSS, el preprocesador estándar de Angular, manteniendo la estética vanilla (sin Tailwind ni frameworks de utilidades). Se centralizaron los valores de diseño en `_variables.scss` (colores, espaciado, radios, sombras) y los estilos de componentes se refactorizaron con anidado y `@use`.
- **Archivos**: `angular.json` (styles + `stylePreprocessorOptions.includePaths`), `src/styles.scss` + `src/styles/_variables.scss` + `src/styles/_base.scss`, 7 `*.component.css` → `*.component.scss` y sus `*.component.ts`
- **Decisión clave**: Todos los componentes consumen tokens de `_variables.scss` vía `@use 'styles/variables'` (resuelto por `includePaths: ["src"]`), evitando colores hardcodeados duplicados.

### 2026-08-07 — feat — Sistema de historial de cambios
- **Descripción**: Se creó la convención de historial (este archivo), la regla de proyecto en `AGENTS.md` y el skill `change-history`, que obliga a registrar cada cambio antes de cerrar la tarea.
- **Archivos**: `HISTORIAL.md`, `AGENTS.md`, `C:\Users\Enilio\.config\opencode\skills\change-history\SKILL.md`
- **Decisión clave**: Ningún cambio se considera terminado sin su entrada en este historial.

### 2026-08-07 — feat — Cascarón del proyecto (creación inicial)
- **Descripción**: Proyecto Angular 15.2 con catálogo público (categorías → productos con precio), carrito persistente y dashboard admin protegido por login. Módulos con lazy loading y guard de rutas.
- **Archivos**: `src/app/` completo (core, shared, home, catalog, cart, auth, admin), `package.json`, `angular.json`, `src/styles.css`
- **Decisión clave**: Los servicios (`CatalogService`, `CartService`, `AuthService`) son la única frontera de datos; hoy devuelven mock en memoria + localStorage y mañana se reemplazan por `HttpClient` contra una API (MongoDB) sin tocar los componentes.
